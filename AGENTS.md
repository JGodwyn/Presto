<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

<!-- BEGIN:presto-agent-rules -->
# Presto — Agent Rules

This file is read automatically by Claude Code, Antigravity, and other AI coding tools that support this format. Keep it short — it loads on every message.

## What this is

Presto is a personal tool that generates, organizes, and schedules social media posts (LinkedIn first, then X). Being built solo, vibe-coded, for personal use first. Public release comes after it's tested.

## Stack — fixed, do not change without asking

- Next.js 14+, App Router
- TypeScript (always — no plain .js files)
- Tailwind CSS
- Supabase — auth, database, file storage
- Vercel — hosting, cron jobs
- Vercel AI SDK + Zod — post generation, streaming, structured output
- shadcn/ui — base components, restyled to match tokens
- React Hook Form — all forms

If a task seems to need a different tool or library, stop and ask instead of adding one.

## Design tokens — strict rule

Exported from Figma as JSON, two files:
- `/design-tokens/foundations.json` — colors, spacing/padding, corner radius, stroke width
- `/design-tokens/typography.json` — font family, size, weight, line height

`tailwind.config.ts` reads its values from these files only.

- Never invent a color, spacing value, corner radius, stroke width, or font size.
- No hardcoded hex codes, px values, or font names in any component.
- If a value you need isn't in the token files, ask — don't guess a close one.
- Any element with a corner radius must use corner smoothing (Figma's "squircle" curve), never plain CSS `border-radius`. Use the `figma-squircle` package via the `useSquircleClipPath` hook (`hooks/use-squircle-clip-path.ts`) — see `components/ui/segmented-control.tsx` for the reference implementation. Keep the matching `rounded-rad-*` class alongside it as the fallback shape before the clip-path is measured on mount.

## Navigation

**Sidebar:** Dashboard, Instructions, Generate, Content, Connections
**Navbar:** user chip, Settings (gear)

Labels follow the Figma designs: "Instructions" (covers both instructions and resources — one combined section, not two) and "Content" (the content-calendar section; route path stays `calendar`).

## Reporting back — keep it short

When a task is done, the reply is a short summary, not a write-up. The user has
said plainly they will not read a long one.

- **Lead with what they must act on**, or would be surprised by. If there's
  nothing, say what changed in a line or two and stop.
- **Flag only what's genuinely notable**: a blocked step, a judgment call you
  made for them, something left undone, a caveat that will bite later.
- **Don't explain what worked.** A change that did exactly what was asked needs
  naming, not justifying. No reasoning, no alternatives considered, no
  play-by-play of how you got there.
- **Say it once.** No restating the request, no summary of the summary.
- The detail belongs in `EXECUTIONS.md` — that's what it's for. Write it there
  and let the reply stay thin.

## Project logs — read and write these

Three append-as-you-go files at the repo root. They are the working memory this
file is too long to hold:

- **`INTERFACE.md`** — every design decision already settled (tokens, component
  inventory, layout rules, motion values, feedback/loading/scroll conventions).
  **Read it before building any UI**; write to it the moment a decision is made.
- **`LEARNINGS.md`** — symptom → cause → rule for everything this project has
  been bitten by. Read the section matching what you're about to touch; add an
  entry the moment something surprises you.
- **`EXECUTIONS.md`** — timestamped log of the steps taken, task by task,
  including dead ends. Append **during** the task, not reconstructed after.
- **`FOLLOWUPS.md`** — work a branch deliberately did *not* do: it belonged to
  another branch's files, needed a decision, or cuts across every table. **Read
  it after `/integrate` has landed everything**, when nothing is in flight and
  cross-cutting work is finally safe. Add an entry rather than reaching into a
  file another live branch owns; delete an entry the moment it's done.

`.claude/hooks/task-log-guard.py` enforces it: once a task has changed five or
more files it nudges mid-task, and the `Stop` hook refuses to end the turn until
EXECUTIONS.md has been updated. Threshold override: `PRESTO_LARGE_TASK_FILES`.

## Browser verification

Use the owner's signed-in Chrome profile through the ChatGPT Chrome extension
for authenticated app testing (`@Chrome`). Use the built-in browser only when
Chrome is unavailable or the task explicitly asks for it.

## Parallel work — branches and worktrees

Several workstreams can be in flight at once, each on its own branch in its own
worktree. Sometimes there are three; often none. The rules:

- **Never write to `main` directly — for real work.** Start it with
  `/branch <slug>` — it creates the branch, a worktree at
  `../presto-worktrees/<slug>`, symlinks the gitignored-but-required files
  (`.env.local`, `design-sync/`, `.claude/settings.local.json`), clones
  `node_modules` copy-on-write, and assigns a free dev-server port from 3001 up.
- **Housekeeping is the exception, and goes straight on `main`.** Small
  post-merge cleanup — a review nit, a stale comment, a one-line fix, a
  FOLLOWUPS entry — when **nothing is in flight** (`./scripts/worktree.sh list`
  reports no worktrees). Edit `main`, run the gates yourself (tsc, eslint
  against `main`'s own baseline rather than zero, vitest, build), commit, push.
  No `/branch`, and **no `/integrate`** — spawning the senior-engineer agent to
  review a two-line fix costs a worktree, an agent cold start and a full review
  pass, and buys nothing when there is no parallel work to collide with.
  The moment anything else *is* in flight, or the change is more than
  housekeeping, it is a branch again — that is what the rule above protects.
- **One branch per task you could describe in one sentence.** If the brief needs
  an "and", it is two branches.
- **`/handoff`** in the worktree when the work is done: gates, logs, commit,
  mark ready. It does not merge.
- **`/integrate`** in the main checkout to land what's ready — runs the
  senior-engineer agent, which reviews, merges the clean branches, deletes what
  is spent, and flags conflicts for you. It never resolves a conflict and never
  pushes.
- **Schema changes are serialized.** Migrations apply straight to the live remote
  Supabase project and *cannot be unmerged*, so only one in-flight branch may own
  the DB at a time (`/branch <slug> feat --schema`; `./scripts/worktree.sh
  schema-owner` says who holds it). While branches are in flight, prefer additive
  changes — a drop or rename takes down every other running dev server at once.
- **Hot files** — `components/ui/*`, `lib/*`, `types/*`, `hooks/*`,
  `components/shared/*`, `app/globals.css`. Two live branches editing these will
  conflict. A cross-cutting branch (one whose whole job is these files) should be
  short-lived and merge *before* feature branches.
- **Say the port out loud** when working in a worktree. Verification here is
  browser-driven, and screenshotting :3000 while the change is on :3002 is the
  easiest way to conclude a working change is broken.
- `EXECUTIONS.md`, `LEARNINGS.md`, `INTERFACE.md` and this file merge with git's
  `union` strategy (`.gitattributes`) so branches can all append without
  conflicting. **Append; never restructure another branch's section.** On a
  `package-lock.json` conflict, regenerate rather than merge:
  `git checkout --ours package-lock.json && npm install`.



## Where the full specs live

- `Presto_PRD_v1_1.docx` — what the product does, feature by feature
- `Presto_UX_Reference_v1_0.docx` — how each screen behaves, states, edge cases

Read the relevant section before building a page for the first time. Don't ask the user to repeat something that's already written down in these.

## Folder structure

```
/app
  /(auth)          → login, forgot-password
  /(signup)        → signup — own route group for its full-bleed branded layout
  /(dashboard)     → dashboard, instructions, generate, calendar, connections, settings
/components
  /ui              → shadcn base components, restyled to tokens
  /shared          → used across more than one page
  /generate, /calendar, /instructions  → feature-specific components
/lib
  /supabase        → client setup, queries
  /ai              → generation wrapper, prompt building
/types
  post.ts
  instructions.ts
/design-tokens
  foundations.json
  typography.json
```

## Shared data shapes

`types/post.ts` and `types/instructions.ts` are the single definition of what a post and an instructions set look like. Every page reads and writes through these two files. Never create a slightly different version of either shape somewhere else — edit the shared one.

## Conventions

- Components: PascalCase, one per file
- Other files: kebab-case
- Writes go through Supabase server actions, not one-off API routes, unless there's a real reason
- Comments explain *why*, not *what* — the code itself should be readable
- Match existing patterns already in the codebase before introducing a new one
- All animation/motion work must be reviewed against Emil Kowalski's course (animations.dev) — see `.agents/skills/review-animations/STANDARDS.md` for the distilled conventions (easing, duration, physicality, `@starting-style`, etc.) and cite them instead of improvising values.
- Any action that takes noticeable time must give the user immediate feedback — never leave them staring at a static screen wondering if their click registered. A loading/pending state (spinner, disabled button, skeleton) for anything that produces new content. For deletions specifically: remove the item from the UI immediately and let the server call finish in the background, rather than waiting for confirmation before removing it — reinsert it and surface a danger Toast only if the delete actually fails. See components/instructions/writing-style-card.tsx's `handleDelete` for the reference implementation, and MyVoiceCard's topic add/remove for the same optimistic-update shape applied to a non-delete action.
- Client-side autosave with no save button that can plausibly fire in a rapid burst (several topic adds/removes, multiple field blurs in quick succession — not a single isolated blur) must go through a save queue, not a bare `void save(...)`: an unguarded fire-and-forget call has no ordering guarantee between requests, so an out-of-order network response can leave stale data persisted even though the UI (and the DB, once corrected) should only ever reflect the latest state. Use `useSaveQueue` (hooks/use-save-queue.ts) — it keeps at most one save in flight and collapses anything queued behind it down to just the latest payload, so a burst becomes a strictly-ordered request stream with no added delay on the common single-save case. See components/instructions/my-voice-card.tsx's `save`/`queueSave` for the reference implementation. A single blur-to-save site with no realistic burst path (e.g. writing-style-entry.tsx's `handleTextBlur`) doesn't need this — a plain awaited call is fine.
- No scrollable area ever shows the browser's default scrollbar — `::-webkit-scrollbar` customization is silently ignored under macOS's overlay-scrollbar rendering path anyway, so always hide it (`HIDE_NATIVE_SCROLLBAR_CLASSNAME`, hooks/use-scroll-thumb.ts) regardless of what else you do. Whether to *also* show a custom thumb is a per-container call: menu.tsx (a dropdown list) and pill-textarea.tsx use one, the page-level `<main>` in app/projects/[projectId]/layout.tsx deliberately doesn't (a thumb positioned against its full width ended up sitting on top of card content at some viewport widths — removed rather than repositioned). Where you do want one: `useScrollThumb` + `ScrollbarThumb` (components/ui/scrollbar-thumb.tsx) — pair the hook's `ref`/`onScroll` with the hide-native class on the scrolling element, and render `ScrollbarThumb` (passing its `thumb` and `visible` — it only shows while actively scrolling, fading ~1s after the last scroll event, never a permanently-visible bar) as a sibling inside a `relative` ancestor that is *not* itself the scrolling element (it needs to stay put as an overlay, not scroll away with the content).

## Publishing is LIVE — read this before touching anything that sends

**Green-lit by the owner on 2026-09-07.** The old blanket rule ("never publish
or schedule to a real connected social account", stated 2026-08-18) is
**superseded**: `PRESTO_ENABLE_LIVE_PUBLISH=true` is set in Vercel **production**,
and the `presto-publish-due` pg_cron job runs **every minute** against the live
origin. A post that comes due now goes out to a real LinkedIn account with
nobody clicking anything.

What this changes for you:

- **Production is not a sandbox.** Scheduling a post for a time in the near past
  or the next few minutes will publish it for real, within the 15-minute grace
  window. There is no undo — `lib/publish-runner.ts` records a URN, it cannot
  retract a share.
- **The gate is deliberately NOT set on Preview**, and must stay that way:
  preview deployments run against this same production database, so setting it
  there would let a throwaway branch publish to a real account. Production only.
- **Turning it back off is an env change plus a redeploy** — a running
  deployment keeps the old env, so deleting the variable alone changes nothing
  until the next deploy. The scheduler can also be stopped outright at the
  database: `select cron.unschedule('presto-publish-due');`
- **X is still off** and this does not change that: `PRESTO_ENABLE_LIVE_PUBLISH`
  is one switch for both platforms, but X additionally needs
  `PUBLISHABLE_PLATFORMS`, `X_SCOPES` and the app's own permission — see the X
  bullet in Current status. Only LinkedIn actually sends today.

Still ask first, every time, before: widening what the scheduler selects
(`platform`, the due window, the batch limit), enabling a second platform, or
setting the gate in any environment other than production. A change that makes
*more* things send is the owner's call, not a judgment call.

## Ask before doing these

- Adding a new npm package
- Changing the database schema
- Swapping out any part of the stack listed above
- Deleting or fully rewriting a file instead of editing it
- Introducing a new UI pattern that doesn't already exist elsewhere in the app

## Current status

*(Update this section by hand as the build progresses — keep it to a few lines, not a changelog.)*

- IA finalized: Instructions covers instructions + resources; Connections moved from Settings into the sidebar
- Generate page: two generation types (Number-based / Calendar-based) behind a segmented control; AI model and social account are chosen right on the page (supersedes the earlier "model choice lives only in Settings" decision)
- Calendar: Kanban + List views; drag-and-drop between columns is Phase 2, not MVP
- Open before high-fidelity design: draft carryover behavior across month boundaries, URL fetch timing on Resources
- Design tokens wired: foundations.json + typography.json fully resolved into app/globals.css (@theme, Tailwind v4 CSS-first, no tailwind.config.ts). Light mode only — foundations.json has no dark-mode values yet. No chart-1..5 colors (not defined in tokens). Open Runde self-hosted in app/fonts/; Phudu via next/font/google.
- Figma → code workflow: REST API and Figma MCP are rate-limited on the Starter plan — don't use them. Instead: run `npm run figma:bridge` (delegates to the standalone `figma-bridge` CLI, extracted to ~/Code/figma-bridge and installed globally + as a Claude Code plugin), user exports frames via the "Figma Bridge" plugin (formerly "Presto Export"), then read `design-sync/<frame-slug>/` (frame.json with token bindings, screenshot.webp, assets/ — PNGs auto-converted to WebP q80 ≤2880px). The plugin's skill covers general mechanics; `.claude/skills/figma-bridge/` keeps the Presto-specific token/component mapping.
- /create-project built from Figma export (first screen after login/signup — no-project empty state, full-bleed, outside (dashboard) chrome). Auth actions redirect there unconditionally until projects exist in the DB. "Create project" CTA now opens a centered modal (components/create-project/create-project-modal.tsx, built on new components/ui/dialog.tsx — @base-ui/react/dialog, follows sheet.tsx's pattern) with a project-name field (RHF + Zod).
- "Your projects" screen built from the Figma export at /projects (app/projects/page.tsx + components/projects/): own full-bleed navbar (layered mono logo, user chip w/ squircle, gear button → /settings) and folder-shaped SVG cards — purple per project, dashed "new project" card that reopens the create-project modal. Folder grid is a fluid card grid (auto-fill minmax(16rem,1fr)) so rows stay flush with both content edges.
- Project → dashboard flow decided and built: the old (dashboard) route group moved to app/projects/[projectId]/ (dashboard, instructions, generate, calendar, connections, settings, profile — all URLs now /projects/<id>/…). The [projectId] layout does the per-project ownership check once (zod uuid guard + fetchProject; no row → back to /projects). Folder cards on /projects link to /projects/<id>/dashboard. Top-level /profile is a real user-level route (the account screen with the picker's navbar and no sidebar — see the entry at the end of this list); /settings is a stub that forwards to it. Root / fans out like login: signed out → /login, else by project count.
- In-project chrome + dashboard empty state built from the Figma "Dashboard" export: [projectId]/layout.tsx reuses ProjectsNavbar (new settingsHref prop points the gear at the current project's settings) beside components/shared/project-sidebar.tsx — white squircle card (widened to w-64 from the Figma frame's 192px, by request), pill nav items (Phosphor bold icons, labels per the Navigation section above), and the 384×930 pixel-gradient artwork (public/images/dashboard/sidebar-gradient.webp, natural aspect at card width) with FolderSimple + project name (line-clamp-3) pinned at the foot. Dashboard page is the "Nothing here" empty state (w-68 column, brand/xl "Get started" — not wired to anything yet; real dashboard replaces it once posts exist). button.tsx now declares "use client" (it always used the squircle hook; server pages importing it used to crash). The old shadcn chrome (shared/navbar.tsx, shared/sidebar.tsx, ui/sidebar.tsx) has been deleted.
- Projects are wired to Supabase: `public.projects` table (id/user_id/name≤80/created_at, RLS per-user, migration `create_projects_table`), shared shape in types/project.ts, reads via lib/supabase/queries.ts, writes via the createProject server action (app/projects/actions.ts). /projects ⇄ /create-project redirect on has-projects/none; login lands by project count; signup still lands on /create-project.
- `cn()` (lib/utils.ts) now uses `extendTailwindMerge` with this project's custom font-size tokens registered — fixes tailwind-merge silently evicting classes like `text-heading-lg` when combined with a `text-text-*` color class in the same `cn()` call (both used to fall into tailwind-merge's default text-color bucket). The bracket-syntax workarounds already in pill-input.tsx/button.tsx/segmented-control.tsx still work (harmless/redundant) but new code can go back to bare utility classes.
- /signup restyled to match Figma (UI only, not wired to Supabase yet — old signup-form.tsx/actions.ts preserved unused in app/(auth)/signup/ for a follow-up task). New reusable primitives: components/ui/segmented-control.tsx, components/ui/pill-input.tsx, Button's new "brand" variant/"xl" size. Signup fields follow Figma (email/name/password) which conflicts with the UX doc's email/password/confirm-password spec — needs reconciling.
- `hooks/use-squircle-clip-path.ts` fixed at the root: switched from `useRef`+`useEffect` to a callback ref. The bug: any element that mounts conditionally after first render (e.g. inside Base UI's `DialogPortal`, which renders `null` until the dialog opens) got its `ResizeObserver` attached to a `null` ref on that first pass, and since the effect's dependency array never changed, it never got a second chance once the real node appeared — clip-path silently never applied. Callback refs fire on every actual attach/detach, whenever that happens.
- Squircle corner smoothing is now retrofitted everywhere per the design-tokens rule above: `Button` applies it automatically per size (no more opt-in prop — `default`/`lg`/`icon`/`icon-lg`→8px, `sm`/`icon-sm`→12px, `xl`/`icon-md`→16px, `xs`/`icon-xs`→4px, mapped from each size's actual rendered radius), plus `pill-input.tsx` (16px), `input-otp.tsx` (16px), `toast.tsx` (12px). All existing Button/pill-input/OTP/toast usages across the app inherit this with no call-site changes.
- /create-project's empty-state content (avatar, greeting, heading, create-project button, subtext) now has a blur+opacity mount-in transition via Tailwind's `starting:` variant (`@starting-style` — no client JS, stays a Server Component), applied once to the whole group as a single unified appearance (no stagger — tried per-element staggering first, felt off), per `.agents/skills/review-animations/STANDARDS.md` (Emil Kowalski / animations.dev conventions: strong ease-out curve, duration-300).
- Onboarding tour built from the Figma "Onboarding 0-5" exports, all under components/onboarding/: OnboardingProvider (onboarding-context.tsx) is the single source of truth — step is `"cover" | 1-5 | null`, read from/written to `localStorage["presto:onboarding:completed"]` — global, not per-project (it's the same tour regardless of which project you're in; OnboardingProvider no longer even takes a projectId prop). Client-only per-browser persistence, no schema change — see the "Tour trigger" decision this was built against. OnboardingCover is the full-viewport accept/decline gate (Figma "Onboarding 0", z-50 over everything); OnboardingTopbar/OnboardingCallout render the steps-1-5 chrome (danger "End onboarding" → success "Complete onboarding" on step 5). components/shared/project-topbar.tsx swaps ProjectsNavbar for OnboardingTopbar mid-tour; ProjectSidebar force-highlights the current step's nav item via `activePath` (tour narrates in place, doesn't navigate). The callout card's vertical position is a calc() formula mirroring the sidebar's own spacing tokens (pad-md/2.5rem-item/dist-md, minus main's own p-pad-lg) rather than a DOM measurement — see the comment on `calloutTop()` in onboarding-callout.tsx if the sidebar's rhythm ever changes. Wired into app/projects/[projectId]/layout.tsx (OnboardingProvider wraps the whole chrome). Step content/copy lives in components/onboarding/steps.ts. Context also exposes `restart()` (jumps straight to `"cover"`, no localStorage clear needed) behind a small outline "Replay onboarding" button (components/onboarding/replay-onboarding-button.tsx, top-right corner of the dashboard empty state) — a dev/testing affordance, not part of the Figma design, so it's deliberately understated.
- Two onboarding fixes per `.agents/skills/review-animations/STANDARDS.md` + the apple-design skill: (1) OnboardingCover's gradient image was rendering blurred — the bug was stacking order, not the image itself: a `backdrop-blur` dim layer was placed *after* (on top of) the image instead of blurring only what's behind the whole cover, so it blurred the image it sat on. Since the image is opaque and edge-to-edge, that dim/blur layer never showed through anyway (matches the Figma screenshot, which is crisp) — removed it outright rather than reordering. (2) OnboardingCallout had no transition at all between tour steps — content just snapped. Fixed with three coordinated pieces, all CSS-only (no animation library added): the card's position is now a `translate` (not `top`, which is layout-triggering) that transitions on every step change (`ease-out`, 300ms); the card's one-time mount (opacity/scale/blur) uses `starting:` so it "materializes" per the apple-design skill rather than a plain fade, and doesn't replay on step changes since `starting:` only fires at DOM insertion; the inner icon/heading/description/button block is `key={step}`'d so each step is a fresh element with its own small `starting:` fade+lift entrance (200ms) — no exit animation, matching this codebase's existing mount-in-only convention.
- Two more onboarding items via Agentation annotations + follow-up: (1) the cover's background image had no mount-in at all (only the text block did), so it popped in solid a beat before the text — gave it the identical `starting:` blur+opacity transition so both fade in together. (2) Settings page gets a "Log out" button (reuses the existing `logout` server action + `LogoutButton` from the profile page — no new component) so the onboarding flow can be tested end-to-end without digging through Profile. (3) The dashboard's empty state was top-aligned instead of centered *behind the tour's blur overlay* specifically — OnboardingCallout's wrapper was `relative flex-1` but not itself `flex`, so the wrapped page's own `flex-1 justify-center` had no flex parent to act against; fixed by adding `flex flex-col` to the wrapper.
- ProjectsNavbar gets a new optional `backHref` prop — an icon-only `brand-secondary` button (matches the gear's style, which also switched from `brand` to `brand-secondary`) next to the logo, only rendered when set. ProjectTopbar passes `backHref="/projects"` for the in-project case; the /projects page itself doesn't pass it, so no back button shows there (nothing to go back to).
- Navigation feedback: folder cards on /projects show a top-left spinner while their project loads (components/projects/folder-loading-indicator.tsx, `useLinkStatus` inside the card's Link — deliberately no loading.tsx at that level so the folders stay visible under the click; 100ms delay so fast navs don't flash). In-project section switches: the clicked sidebar item highlights optimistically (pendingPath state in ProjectSidebar, cleared when usePathname commits; tour's activePath still wins) and app/projects/[projectId]/loading.tsx shows a centered spinner while the incoming section streams in.
- Instructions persistence wired: `public.instructions` table (one row per project, RLS mirrors projects' per-command policies + project-ownership check on insert; migration `create_instructions_table`), shared shape in types/instructions.ts (replaced the stale InstructionsSet placeholder), fetchInstructions in queries.ts, saveInstructions server action in app/projects/[projectId]/instructions/actions.ts. No save button by design: blurring any My-voice field or flipping the single-prompt toggle upserts; failures surface a danger Toast. Field values live in react-hook-form's store so they survive the single-prompt remount and eye collapse/expand (RHF restores uncontrolled inputs on remount, shouldUnregister defaults false). Topics column exists but the topic search is still cosmetic.
- "Add a style" opens the writing-style modal (components/instructions/writing-style-modal.tsx, from the Figma "Instruction Modal - Type/URL/Upload" exports): one dialog, SegmentedControl swaps Type/URL textareas and an Upload dropzone (file picker shows the chosen name only). Save just closes — persisting styles + listing them on the card is the next piece, same for the References modal. DialogContent gained an optional `popupClassName` (the Popup owns width; className styles the inner card) for the modal's w-90.
- Instructions page UI built from the Figma "Instructions" export (UI only — no Supabase yet): three cards under components/instructions/ (InstructionsCard shell + DottedDivider + MyVoiceCard; the Writing-style/References empty-state cards are composed inline in the page from the shell + brand/sm Button). New primitives: components/ui/switch.tsx (Base UI Switch styled to the Figma Toggle, squircle track+knob) and components/ui/pill-textarea.tsx (TextArea twin of pill-input). My-voice fields (Tone/Content rules/Post structure/What to avoid) expand/collapse via eye buttons (open eye = collapsed, closed = expanded, per the Figma frame); placeholders for the non-Tone fields come from UX doc §6. "Use single prompt" swaps the sectioned fields for one tall textarea — invented interpretation, not in the export. Topic search input and both "Add a…" CTAs render but do nothing yet.
- Topics are functional: components/ui/chip.tsx + components/ui/menu.tsx built from the "Chip"/"Menu"/"menu-item" Figma exports (Menu uses the toast.tsx two-div trick — drop-shadow on an unclipped wrapper, squircle clip + border on the inner card; MenuItem's `highlighted` prop renders the export's focused state, used for keyboard-active options). components/instructions/topic-picker.tsx is a hand-rolled combobox (no new package): opens on focus with COMMON_TOPICS (hardcoded list in that file), filters as you type, shows an `Add "…"` row (input portion truncates, closing quote stays) when nothing matches, arrow/Enter/Escape keyboard nav, aria combobox/listbox roles; picked topics render as wrapping removable Chips. Selection + saving live in MyVoiceCard (topics useState, explicit-next save like the toggle); saveInstructions schema/upsert now include topics (each ≤80 chars, ≤100). Also: each My-voice field's whole label row (text through eye icon) is now one `<button>` inside the h3 — bigger toggle target, single tab stop.
- ProjectFolder (the /projects grid card) picked up two more interactions, both plain CSS via a `group` class on its wrapping `<Link>` — no "use client" needed: (1) press feedback, `group-active:scale-[0.97]`, on its own 150ms ease-out transition (per the animation standards' button-press duration, not the 300ms the entrance opacity/scale already used — split via one arbitrary `[transition:scale_150ms_ease-out,opacity_300ms_...]` declaration so entrance and press don't have to share a duration). (2) a "Created <date>" caption (ProjectFolder now takes `createdAt`, formatted with `toLocaleDateString`) absolutely positioned top-left, revealed via `group-hover:opacity-100` — no Tooltip primitive; components/ui/tooltip.tsx exists (shadcn default, unstyled) but isn't used anywhere yet.
- Writing-style entries are wired to Supabase, closing out the "next piece" noted above: `public.writing_styles` table (many rows per project — id/project_id/user_id/kind `text|url|file`/content/file_name/file_size/file_path/created_at, RLS mirrors instructions' per-user pattern, migration `create_writing_styles_table`) plus a private `writing-style-files` Storage bucket (`create_writing_style_files_bucket`, objects keyed `${userId}/${projectId}/${uuid}-${filename}`, RLS checks `storage.foldername(name)[1] = auth.uid()`). Shared shape in types/writing-style.ts, fetchWritingStyles in queries.ts, add/delete server actions in app/projects/[projectId]/instructions/writing-style-actions.ts (file adds go through FormData per the Next.js Server Actions guide, not JSON). WritingStyleModal now persists on Save and takes a `compact` prop — full labeled trigger for the empty state, icon-only "+" once the card has entries (Figma "My writing style (Content added)" export put the button in the header row, not below the divider). components/instructions/writing-style-card.tsx owns the list as client state (seeded from the server fetch, appended/filtered locally on add/delete — no re-fetch, same pattern as MyVoiceCard's topics) and components/instructions/writing-style-entry.tsx renders each kind's preview (text/url as a surface-rest box, file as a surface-3 card — see the FileTypeIcon entry below for how the file icon itself is drawn). InstructionsCard gained an optional `headerAction` prop for this title-row button. URL entries are clickable (open in a new tab) — a small addition beyond the export, not spec'd either way.
- Writing-style follow-ups: (1) Text entries are now editable in place — a live PillTextarea saving on blur via a new `updateTextWritingStyle` server action, scoped to `kind = "text"` in the query as well as in the UI (required adding an `UPDATE` RLS policy, migration `add_writing_styles_update_policy`; URL/File stay display-only by design). (2) Delete is optimistic per the feedback convention above. (3) components/instructions/file-type-icon.tsx replaced the Phosphor per-extension icon with the real Figma "FileTypes" export: one shared base illustration (public/images/instructions/file-icon-base.webp, identical raster for every extension) plus a colored extension tag layered on top, using the export's literal gradient/border hex per extension (pdf/doc/docx/txt only — the ones this app actually accepts; not a design token, same exception as the dashboard's pixel-gradient artwork). (4) The writing-style entry header's File icon is Phosphor `File`, not the reused `Link` icon the Figma frame.json literally specifies for it (reads wrong for an attachment). (5) The upload dropzone shows a green `CheckCircle` (weight="fill", `text-icon-success` — its hex happens to equal the Figma "Instruction Modal - Upload successful" export's checkmark exactly) and bolds the filename once a file is picked. (6) Trash icons (writing-style entries) and the My Voice eye/eyeclosed icon now rest at `icon-subtle`, going to `icon-danger` (trash) / `icon-bold` (eye) on hover — the icon-* token namespace, not text-*, since these are icons not text.
- Custom scrollbar, app-wide: `::-webkit-scrollbar` styling is silently ignored under macOS's overlay-scrollbar rendering path (discovered building components/ui/menu.tsx's dropdown), so every scrollable area now uses a real thumb element instead of the native one — extracted into `useScrollThumb` (hooks/use-scroll-thumb.ts) + `ScrollbarThumb` (components/ui/scrollbar-thumb.tsx). menu.tsx was refactored onto the shared hook (no behavior change), the project layout's page-scroll `<main>` is now components/shared/scrollable-main.tsx, and pill-textarea.tsx grew the same thumb over its native textarea scrollbar (composes its own scroll-tracking ref with a caller-supplied `ref`, e.g. react-hook-form's `register()`, so nothing downstream broke). See the Conventions entry above — new scrollable containers should reach for this pair rather than the browser default.
- The scrollbar thumb went through two more iterations after landing (both per direct feedback): first made visible-on-scroll-only (faded on a `visible` flag, cleared ~1s after the last scroll event), then removed outright from the page-level scroll container specifically — at some widths it ended up sitting on top of the References column instead of staying in the page's outer gutter ("bleeds into the page"). components/shared/scrollable-main.tsx was deleted; the project layout (app/projects/[projectId]/layout.tsx) now renders a plain `<main>` again, just with `HIDE_NATIVE_SCROLLBAR_CLASSNAME` so there's no native scrollbar either — scrolling itself works fine via wheel/trackpad/keyboard, there's just no visual indicator. `useScrollThumb`'s scroll-triggered fade is still the standing behavior for the scrollable areas that *do* keep the custom thumb (menu.tsx, pill-textarea.tsx) — see the Conventions entry.
- ProjectSidebar (components/shared/project-sidebar.tsx), after a couple of follow-up corrections: the bottom-pinned gradient image is rendered *before* `<nav>` in the JSX (both are positioned elements with the default z-index:auto stacking order, so DOM order alone decides paint order) — nav, being later, always paints over the image, so the image can never visually cover the tabs, at any height, with the image kept at its original unclipped/unwrapped size and position (an earlier `flex-1 overflow-hidden` wrapper approach was reverted — it visibly cropped the image, which wasn't wanted). Below nav sits a hardcoded (explicitly authorized, not a design token) `min-h-20 flex-1` spacer, then the folder-name block as a normal in-flow child (`relative` — dropping `position: absolute` here without adding `relative` made it paint *behind* the still-absolute image and disappear entirely, since static elements paint before positioned ones regardless of DOM order). The `flex-1` on the spacer still lets it soak up extra room in a tall viewport (pushing the folder-name block down to the card's true bottom edge, same visual position as before), while `min-h-20` stops it going below 80px. `<aside>` keeps `min-h-max`: since nav, the spacer, and the folder-name block are now all real in-flow content, max-content correctly sums all three + padding on its own — no manual height arithmetic. Verified end-to-end (gap, no-overlap, folder-name visibility) by forcing `<aside>`'s height down via devtools rather than a real narrow-viewport screenshot (couldn't get the actual browser window under a few hundred px in this environment).
- References is wired to Supabase, built as a straight mirror of My writing style — same UI, same interaction model, different table: `public.content_references` (migration `create_content_references_table`, RLS identical shape to `writing_styles` including the `UPDATE` policy from day one this time) plus a private `content-reference-files` Storage bucket (migration `create_content_reference_files_bucket`). Shared shape in types/content-reference.ts, fetchContentReferences in queries.ts, add/update/delete server actions in app/projects/[projectId]/instructions/reference-actions.ts. components/instructions/reference-modal.tsx, reference-entry.tsx, and reference-card.tsx are line-for-line structural copies of the writing-style-*.tsx trio (same Type/URL/Upload tabs via the same shared UploadDropzone and FileTypeIcon components, same optimistic-delete/save-queue-free blur-save/compact-trigger behavior) with only the copy and target table swapped — modal title "Reference", Type/URL placeholders reworded for reference material rather than post examples. page.tsx's inline References empty-state block was replaced with `<ReferenceCard>`, fetched alongside the other two cards via `Promise.all`.
- Important framework gotcha found while fixing large-file uploads: Next's Server Actions have a **default body-size limit of exactly `1024 * 1024` bytes** (node_modules/next/dist/server/app-render/action-handler.js — not configurable-looking, it's the silent default with no `serverActions.bodySizeLimit` set), and a request over that gets rejected by the framework itself as an opaque error *before* any of our own validation code runs. The old 10MB checks in writing-style-actions.ts/reference-actions.ts were therefore dead code for anything between 1MB and 10MB. Fixed by making 1MB the real, intentional limit everywhere: components/instructions/upload-dropzone.tsx (shared by both writing-style and reference modals) rejects a file client-side the instant it's picked — never calls `onFileChange`, so it can't reach Save — showing `border-border-danger` on the dropzone (if/else against the focus-border class, not both at once, same pattern as pill-input.tsx's danger state — see the code comment for why) plus "Keep your files less than 1MB" underneath; both server actions' `MAX_FILE_BYTES` and error copy were brought down to match as a defense-in-depth backstop. If this limit is ever raised, both the client check in upload-dropzone.tsx *and* `next.config.js`'s `serverActions.bodySizeLimit` need to move together, or the framework ceiling silently becomes the real (lower, unexplained) limit again.
- Generate page (number-based) built from the Figma "Generate (Number based)" export, UI only — no generation wired: page.tsx renders GeneratePanel (white squircle panel over the canvas; the green pixel-glow art is one image mirrored top/bottom at `calc(100%*1157/920)` of panel width — the export's exact wider-than-container crop; static info marker top-right, deliberately not a button since it has no behavior yet) + GenerateCard (components/generate/) — Number/Calendar segmented tabs, the display-lg −/count/+ stepper (NumberStepper, range 1–31 — a month of dailies, raised from the UX doc §8.2's 20 by request; the count animates via `@number-flow/react`, added at the user's direction; the count box has exactly two widths — 1ch single-digit, 2ch double, transitioned on the 9↔10 crossing — after both a fixed 2ch (single digit looked marooned) and NumberFlow's free intrinsic width (buttons drifted on every step) were tried and rejected — its roll-mask padding is absorbed by pinning the count box to the display-lg line-height token so layout stays put; the button-to-count connectors extend dist-md *under* the buttons so the press scale-down can't open a gap, and dim in sync with their button's disabled state), and two SelectPill capsule dropdowns for AI model + social account (portaled Menus, same clip-path reason as TopicPicker; options are hardcoded placeholders until Settings/Connections feed them). All state local. The Calendar-based tab currently holds a provisional demo of the new Calendar component (below), not the real Daily/Monthly design — that's still the agreed next piece (Daily range/multi-date picking, Monthly month-picking, per-date exclusions, clear/reset).
- `components/ui/calendar.tsx` — a from-scratch `Calendar` built off four Figma exports (design-sync/calendar, calendar-title-bar, calendar-item; calendar-action-bar not used yet — see below). One card, three internal views: `day` (default) / `month` / `year`, switched by clicking the title bar's "{Month}. {Year} ⌄" (Day→Month→Year→Day is the only flow a single dropdown affordance supports — there's no independent year trigger). `size` is `"sm" | "md" | "lg"` (all three Figma variants implemented, default `"lg"`, dimensions read straight off the export via Tailwind's 4px-unit scale — e.g. `w-80`=320px). `mode` is `"single" | "range"`; range is a plain two-click picker (first click starts fresh, second commits, swapped into from/to order if picked backwards) with a live hover preview of the pending in-between days (the Figma "range" cell state, reused for both the committed range and the pre-commit preview). Month grid is real date math (`getMonthGrid`), not the export's mock (which starts every month on a Sunday and skips two days — clearly placeholder data); adjacent-month days render in the export's "disabled" look and are non-interactive. Month/year lists auto-scroll the current entry into view on open and reuse the app's standard custom-scrollbar pair (`useScrollThumb`/`ScrollbarThumb`). No Apply/Cancel footer — `design-sync/calendar-action-bar` is a separate export, left for whatever feature composes this in later (it already maps 1:1 to existing `Button` variants: action-bar `Size=sm`→Button `size="sm"`, `Size=lg`→Button `size="xl"`). Verified end-to-end in-browser: real-month rendering, range commit + hover preview, disabled-cell non-interactivity, and the Day→Month→Year→Day flow with auto-scroll.
- Three follow-up fixes on the Calendar, all per direct feedback: (1) clicking the pending range-start date a second time is now a no-op rather than committing a same-day `from === to` range (was producing "July 23, 2026 – July 23, 2026" in the demo's readout). (2) The card content (title bar + body) now sits inset by a real `p-pad-md`, restoring padding the Figma card component actually specifies (`design-sync/calendar`'s own frame padding) that the first pass had missed — the title bar no longer runs flush to the card's rounded edge. (3) Re-verified the corner-radius fix from the prior round by comparing the card's and border-overlay's clip-path strings and bounding rects in the live DOM — both are byte-identical, confirming the two squircle-clipped layers share the exact same shape with no sub-pixel mismatch left.
- The real Generate calendar-based screens are built, from five Figma exports (design-sync/generate-calendar-based-*): Daily/date-range, Daily/pick-dates (+ its picked and empty-selection-error states), Monthly/pick-months, Monthly/skip-some-dates. Replaces the earlier single-Calendar demo in generate-card.tsx's calendar-based branch.
  - `components/ui/calendar.tsx` gained a third mode, `"multiple"` (`selected: Date[]`) — plain toggle-membership, no ordering or range fill. Serves both "Pick dates" (starts empty, click adds) and the Monthly skip-dates calendars (starts as every day of that month, click moves it into a separate `skippedDates` array — `GenerateCalendarColumn`'s `MonthSkipCalendar` diffs Calendar's before/after array each click to figure out which single day flipped, since Calendar itself only knows "toggle this date's membership").
  - New components/generate/ files: `radio-card-group.tsx` (Base UI `Radio`/`RadioGroup` styled to the Figma "Posting cadence" / "How do you want to select dates?" cards — both share this exact shape, label+options only difference), `scheduled-posts-bar.tsx` (dark counter pill, count animated via NumberFlow), `month-grid.tsx` (year nav + flex-wrap month toggle pills — visually identical to `components/ui/chip.tsx`'s selected/unselected treatment but a real `<button>` since these toggle directly rather than being added via a search field, so the shared Chip component wasn't reused/touched), `generate-calendar-column.tsx` (the whole right column: info line + Calendar/MonthGrid, the skip-dates toggle + horizontal-scrolling row of per-month skip calendars, and the "Select dates here first" validation error).
  - `generate-card.tsx` now owns all calendar-based state (cadence, date-select-method, daily range/dates, selected months, skip-dates enabled + skipped dates) and switches its own top-level wrapper between a single centered `w-110` column (number-based) and a wider two-column row — settings left (`w-110`, same content position as before) + `GenerateCalendarColumn` right — matching the export's 440+16+464px row exactly (`max-w-230`). Scheduled-posts count is computed per mode (range: inclusive day count; pick/skip: array/set lengths) and drives both the counter bar and the Generate button's Post/Posts pluralization (the exports are inconsistent on this — some show singular at 0, one shows singular at 31 — so plain English pluralization was the rule actually coded, not literal export text). The empty-selection error only shows after a Generate click with nothing selected, and clears itself the moment a valid selection exists (no separate reset logic needed — it's gated on `showError && !hasCalendarSelection` at render time).
  - Verified end-to-end in-browser: Pick-dates toggling and count, Date-range inclusive count (7→22 = 16), Monthly month-toggling and count (July+August = 62), skip-dates decrementing the count exactly (62→60 after skipping 2 days), the error message appearing/clearing, and Number-based mode's original centered layout still intact.
- Layout fix on the Generate card, from design-sync/genrate-calendar-based-layout (a corrected re-export — the first calendar-based pass mispositioned the header): title/tabs/info-line were living inside the 440px-wide left settings column, which sits at the *left* edge of the wider two-column row rather than centered in it — so the header visibly jumped left switching from Number-based to Calendar-based. Fixed by hoisting title/tabs/info-line out to the outer wrapper (now a constant `w-230` `mx-auto` column in both modes, per the corrected export's own structure — a shared 920px-equivalent box with the header centered in *that*, not in whichever mode-specific content happens to render below it); only the content below the header now branches between a single centered `w-110` block (number-based) and the left-settings/right-calendar row (calendar-based). `modelAccountAndActions` (pills, Generate button, plug footer — identical in both modes' exports) is now a single JSX block both branches place, rather than living inside one shared column element. Verified by measuring the title's `getBoundingClientRect()` center in both modes — identical to the pixel (1168) before and after switching tabs.
- Six more Generate fixes, per direct feedback and annotations: (1) the range readout ("July 9, 2026 – July 20, 2026" / "No range selected") that lived in the old provisional demo got dropped when the real calendar-based layout was built — restored below the calendar, range mode only. (2) `SelectPill` (components/generate/select-pill.tsx) changed from `bg-surface-2` to `bg-surface-3` — affects both the model and account pills, one shared component. (3) The model pill's pencil icon is now the same rotate-on-open `CaretDown` the account pill already used (a pencil read as "edit," not "open this dropdown"). (4) `GenerateCalendarColumn`'s root is now a fixed `w-100` (400px, `shrink-0`) instead of `w-full` — it no longer changes size depending on whether it holds the 320px Daily calendar, the wider month grid, or several skip-dates calendars. (5) The skip-dates calendar row gained click-and-drag horizontal scrolling (plain pointer events, a 4px movement threshold before it commits to "this is a drag" rather than a click — otherwise tapping a day in a `MonthSkipCalendar` would get swallowed) on top of the wheel/trackpad scrolling `overflow-x-auto` already gave it; its viewport stays clamped to the fixed column width regardless of month count (verified: `scrollWidth` 750 vs `clientWidth` 400 with 3 months selected, and a 200px drag moved `scrollLeft` from 0 to 286).
- Three more fixes to the Monthly skip-dates carousel, per direct feedback: (1) `components/ui/calendar.tsx` gained a `disableNavigation` prop — drops the month dropdown and prev/next chevrons entirely, leaving a plain "{Month}. {Year}" label; day-cell selection still works. Each `MonthSkipCalendar` now passes it, since its displayed month is dictated by the month grid above it, not by navigating the calendar itself. (2) Each of those calendars also gets `className="shrink-0"` so the row's default flex-shrink never squeezes them narrower than their real 320px — confirmed via `getBoundingClientRect()` on all three cards in a 3-month selection: exactly 320 each. (3) The carousel row itself now bleeds one calendar-width-plus-gap (336px, `w-[calc(100%+336px)] -mr-84`) past the fixed 400px column's right edge, so more of the row is visible before a scroll/drag is needed — the left edge stays anchored (only `margin-right` moves), and since nothing sits to the column's right, this doesn't disturb other layout; GeneratePanel's own `overflow-hidden` further up is what finally clips it if the carousel is ever wide enough to reach the true page edge (confirmed no page-level horizontal scrollbar appears — `document.documentElement.scrollWidth` matches `window.innerWidth` exactly).
- Four more fixes to the Monthly skip-dates carousel, per direct feedback: (1) A short month like February 2026 (28 days starting on a Sunday — exactly 4 grid rows, no padding days at all) sits in the same flex *row* as taller months, whose default `align-items:stretch` stretches every card to match the tallest sibling; the calendar's own inner card didn't fill that stretched height (`h-full` wasn't set), so the border sat past where the (shorter, unstretched) white content visually ended. Fixed with two changes in `components/ui/calendar.tsx`: the inner `cardRef` div now has `h-full` so it genuinely fills whatever height the outer (possibly stretched) wrapper ends up at, and the day-view wrapper is now `flex-1 justify-center` so the header+grid block centers in that space rather than leaving a dead gap only at the bottom. (2) The carousel's own scroll measurement (`maxWidthPx`, added last round to cap the right-edge bleed) was computed in a `useLayoutEffect` keyed to a plain `useRef` — since the row only mounts once "Skip some dates" is on *and* a month is selected, that effect fired once against a null ref on `GenerateCalendarColumn`'s own mount and never got a second chance, leaving the bleed unclamped and the true end of the carousel unreachable by any amount of scrolling. Switched to a callback ref (same fix pattern as `use-squircle-clip-path.ts`) so it re-measures on every real attach — verified the last calendar's right edge now lands exactly on the scroll container's own right edge at max scroll. (3) Added `select-none` to the row so dragging never highlights the day numbers underneath the pointer. (4) Built elastic/rubber-band overshoot: dragging past either scroll edge now moves an inner content wrapper via a resisted `translateX` (35% of the raw overshoot, capped at 72px) while the real `scrollLeft` stays clamped to its true range throughout, snapping back to 0 on release via a 200ms transition on the same strong ease-in-out curve used elsewhere (e.g. SegmentedControl's indicator) — verified via dispatched pointer events that mid-drag transform reaches −70px and settles back to 0px after release.
- Two more skip-dates carousel fixes, per direct feedback that the previous round's fixes weren't actually enough: (1) The carousel's right-edge measurement was approximating available space as `window.innerWidth - rowLeft`, which ignores the page's own responsive padding and the sidebar — at any viewport narrower than the automation browser's own (wide) test window, this overestimated how much room actually existed, so the row's bleed still claimed space that GeneratePanel's `overflow-hidden` was quietly cropping, leaving the last calendar unreachable exactly as reported. Fixed by giving GeneratePanel's `<section>` a `data-clip-boundary` marker and having the carousel's measurement walk up to it via `closest()`, measuring against its real `getBoundingClientRect().right` instead of the viewport — confirmed at a realistic 1440px window (where the flawed calc had given 736px of claimed width against only 532px actually available): the fix correctly measures 532px, and the last calendar's right edge now lands exactly on the row's own edge at max scroll, safely inside the panel. (2) Every calendar's drop-shadow was getting a hard flat cutoff at its own top and bottom — not a bug in the shadow itself, but a CSS Overflow Module rule: setting `overflow-x: auto` on the row implicitly forces `overflow-y` to `auto` as well (an axis can't be left "visible" once the other is a scrolling value), which clipped the shadow's vertical bleed on every card equally. Fixed with `py-5` on the row (room for the shadow to render before hitting the clip) paired with a canceling `-my-5` so the extra padding doesn't change the row's footprint in the surrounding layout.
- Three more skip-dates carousel fixes, per direct feedback: (1) The edge-fade mask (previous round) faded directly against the row's own boundary — exactly where a *resting*, unscrolled calendar's own edge sits too, so it visibly dimmed calendars that weren't mid-scroll. Added `px-6` (24px, matching `EDGE_FADE_PX` — kept in sync by hand, Tailwind needs the literal class) with a canceling `-ml-6` so the first calendar still lines up with "Choose dates you want to skip" above it while the fade now only reaches actual mid-scroll content. (2) `selectedMonths` was appended-to in click order, so the carousel (and the month grid's own pill order) showed whatever sequence the user happened to click in rather than calendar order — `handleToggleMonth` (generate-card.tsx) now sorts by `(year, month)` on every toggle, add or remove. (3) Built enter/exit animation (scale-down + blur, 300ms, the same strong ease-in-out curve used elsewhere) for calendars joining/leaving the carousel. React has no built-in exit-animation support — a `useCarouselPresence` hook keeps a locally-owned item list that lags `selectedMonths` by one exit: a removed month is marked `exiting` (still rendered, now carrying the exit classes) and only actually dropped once its transition completes. That completion is normally driven by `onTransitionEnd`, but testing surfaced a real gap worth documenting: browsers suspend transitions (and the events marking their completion) for backgrounded/hidden tabs, so an item deselected right as the tab loses focus can get stuck mid-fade forever with `onTransitionEnd` alone — fixed with a `setTimeout` fallback (350ms) that force-removes the item regardless, cancelled the instant the real removal happens via whichever path gets there first. Calendar's own outer wrapper (`components/ui/calendar.tsx`) needed `h-full` added too — the per-item animation wrapper sits between the carousel row and the calendar, breaking the direct flex-parent relationship the February height-stretch fix (a few rounds back) depended on; `h-full` carries the stretched height down through that extra layer.
- Two reported bugs ("removing the last month doesn't animate, it snaps" / "the carousel rearrange after a removal is snappy, not smooth") traced to one root cause, not two separate animation gaps: `GenerateCalendarColumn` was passing `useCarouselPresence` a fresh `[]` literal every render whenever `skipDatesEnabled` was false (`skipDatesEnabled ? selectedMonths : []`). Since that hook's effect depends on the array *by reference*, a new-but-empty array on every render meant the effect never stopped re-firing — an infinite `setState`-in-`useEffect` loop (React's "Maximum update depth exceeded", confirmed actively spamming the console the entire time the Monthly/calendar-based view was open with the toggle off, i.e. most of the time). React bailing out of that runaway loop is what made animations look like they were snapping instead of playing. Fixed with a module-level stable `NO_MONTHS: MonthSelection[] = []` constant used in place of the literal — same category of bug as "an inline `{}`/`[]` prop breaks memoization," just biting a `useEffect` dependency instead. Also added a `useFlipReorder` hook (FLIP technique) so remaining calendars animate smoothly into their new slot when one is removed.
- Follow-up round on the same carousel, per direct feedback that the last-item snap could still happen ("mostly when I've already popped some out of the list and now is popping the last one") plus a reported jitter ("the one closest to it moves forward and then back") and a general "make it quicker" ask. Root cause of the *still* snapping: `useFlipReorder`'s reorder animation set `node.style.transition = "transform 150ms ..."` as an **inline style** on the same item wrapper that also carries the Tailwind class `transition-[opacity,filter,scale] duration-200` for its own exit/enter fade — inline `transition` (the shorthand) fully replaces a class-based one for the same element, not merges with it, since both target the identical CSS property. So once an item had been through even one reorder, its wrapper's `transition` stayed permanently pinned to `transform` only, silently disabling that item's *own* future exit fade — exactly matching "snaps mostly after some have already popped out" (only items that had survived at least one prior reorder were affected). Fixed by rewriting `useFlipReorder` to animate via `element.animate()` (Web Animations API) instead of a CSS transition — WAAPI runs independently of the `transition` property entirely, so a reorder and a fade on the same element can no longer collide (per `.agents/skills/review-animations/STANDARDS.md`'s own recommendation: "WAAPI gives JS control with CSS performance... no library"). The same rewrite fixes the "moves forward, then back" jitter: the hook now cancels any still-in-flight reorder `Animation` on a node before starting a new one, since two overlapping reorders previously fought over the same transform and corrupted the next position measurement (it would read a mid-flight position instead of the settled one). Durations also dropped per the "generally quicker" ask: `REORDER_DURATION_MS` 200→150ms, `EXIT_ANIMATION_MS` 300→200ms (with the paired literal Tailwind class `duration-300`→`duration-200` at the call site, kept in sync by hand same as `EDGE_FADE_PX` elsewhere in this file) — this fires on ordinary list add/remove rather than a rare first-time moment, so it now sits in the review-animations "small popover" range instead of the page-level mount-in convention used elsewhere in the app. Verified: no stray inline `transition` left on the item wrapper after a reorder (checked via `element.style.transition` immediately before and during a subsequent removal — empty string both times, vs. the old bug's stuck `"transform 150ms ..."`), and the last item now correctly carries the exiting classes through a real intermediate frame (`hasExitingClasses: true`, item still mounted) at the first checkpoint after click, even after two prior removals in the same session.
- Three more items, per direct request: (1) The skip-dates carousel's height-matching (from a few rounds back — every calendar in the row stretched to match its tallest sibling) is reverted back to variable/hugging height by direct request — `components/ui/calendar.tsx`'s outer wrapper and inner `cardRef` div lost their `h-full`, and the day-view wrapper lost its `flex-1` (both added specifically for the stretch behavior, now removed rather than left dormant), and the carousel row itself (`generate-calendar-column.tsx`) gained `items-start` so the flex default of `align-items:stretch` no longer stretches cards to begin with — verified February (4 grid rows) and March (5 rows) now render at genuinely different heights (280px vs. 320px) side by side. (2) A `PaintBrushHousehold` icon (Phosphor) sits next to `GeneratePanel`'s existing static info icon, top-right — `GeneratePanel` gained an optional `onResetCalendar` prop (only rendered when passed, same convention as `ProjectsNavbar`'s `backHref`). Since `GeneratePanel` wraps `GenerateCard` as `children` and has no other way to reach into that self-contained child's internal state, `GenerateCard` is now wrapped in `forwardRef` exposing a narrow `GenerateCardHandle` (`{ resetCalendar: () => void }`) via `useImperativeHandle` — this is a genuinely new pattern in the codebase (no prior `forwardRef`/`useImperativeHandle` usage), chosen over lifting all ~8 pieces of calendar state up to the page specifically because it's the more surgical option and doesn't touch `GenerateCalendarColumn`'s already-large prop surface. `resetCalendar` only resets calendar-scoped state (cadence, date-select method, daily range/dates, month year, selected months, skip-dates enabled + skipped dates, the error flag) — deliberately not `mode`/`count`/`model`/`account`, since the ask was "reset the calendar selection," not "reset the whole form" (confirmed the button stays on Calendar-based mode with model/account untouched after a click). `app/projects/[projectId]/generate/page.tsx` is now a client component (previously a plain server component) so it can hold the connecting ref. (3) Two annotations resolved: the gap between "Skip some dates" and "Choose dates you want to skip" tightened from the column's own `gap-dist-xl` (24px) to `dist-md` (8px) via `-mt-dist-lg` on the paragraph (cancels exactly 16px, the same "kept in sync by hand" cancelling-margin approach used elsewhere in this file); the month grid's year label switched from `text-title-lg font-bold` to `text-heading-sm font-display`, matching every other `heading-sm` usage in the app (all of which pair it with the Phudu display font, not a bare font-weight override).
- BYOK AI models are built (**they live on Settings, not Connections** — see the last bullet in this list): users add their own AI models, replacing the hardcoded single-model setup. Routed through the **Vercel AI Gateway** — `ai@7` already re-exports `gateway`/`createGateway` from `@ai-sdk/gateway` (a transitive dep), a plain `"creator/model"` string routes through it automatically, and provider-executed tools still pass through, so `google.tools.urlContext({})` survived and **no npm package was added**. `@ai-sdk/google` stays installed purely for that tool factory and the built-in model.
  - **What the user provides:** a provider (dropdown from the gateway's own catalog), a raw provider API key (Google/OpenAI/Anthropic — *not* a Vercel key, so no Vercel account needed), and a model picked from the models that key unlocks. The key is forwarded per request as `providerOptions.gateway.byok: { [providerSlug]: [{ apiKey }] }`, so the user's own account pays for inference.
  - **Keys are encrypted at rest** — `lib/ai/key-crypto.ts`, AES-256-GCM via `node:crypto`, stored as `v1.<iv>.<tag>.<ct>`, keyed on a new server-only `MODEL_KEY_ENCRYPTION_KEY` env var (also new: `AI_GATEWAY_API_KEY`; both in `.env.local.example`). GCM not CBC so a tampered row throws instead of silently yielding a garbage "key". The plaintext key never reaches the browser: `fetchUserAiModels` deliberately omits `encrypted_key` from its `.select()`, and the UI shows only the `key_last_four` column.
  - **The gateway's silent-fallback trap, and the guard for it.** Request-scoped BYOK is a *preference*, not a requirement — the gateway's own docs say a request "may still fall back to use system credentials if the provided credentials fail", and there is no request flag to forbid it. So a user's expired key would quietly bill *this app's* gateway account. The only detection is after the fact: `didFallBackOffByok` (lib/ai/generate.ts) reads `providerMetadata.gateway.generationId` off the response and asks `gateway.getGenerationInfo({ id })` whether `isByok` was true. Checked twice — at add time (`verifyProviderKey` in model-actions.ts runs a real `maxOutputTokens: 1` call and rejects a key that "worked" on our credits) and after every BYOK generation (post-actions.ts flips the row to `status = 'error'`, non-fatally, so Connections tells them to replace it). Deliberately fails *open* on an indeterminate result: flipping a working model to an error state on a flaky metadata lookup is worse than missing one.
  - **Schema:** `public.user_ai_models` (migration `create_user_ai_models_table`) — the first table keyed on `user_id` **alone, with no project_id**, because an API key belongs to the person, not a project: add a model once, use it in every project. RLS is four per-command policies on `auth.uid() = user_id` (writing_styles' shape minus the project-ownership subquery). Shared shape in `types/ai-model.ts`.
  - **The model id is no longer an enum.** `GENERATION_MODELS` became `BUILTIN_MODELS` (just `gemini-3.6-flash` + `tastetest`), and the value the Generate pill carries is now *either* a built-in id *or* a `user_ai_models` row uuid. `lib/ai/resolve-model.ts`'s `resolveModelSelection` is the single gate — it lives in `lib/ai/` rather than the actions file because it takes a `SupabaseClient`, which can't cross a `"use server"` boundary. `generatePost` finally takes a `model: ModelSelection` (`{kind:"builtin"}` | `{kind:"byok",…}`); the built-in branch is byte-for-byte what it was, so that path can't regress. New `model_unavailable` failure reason for an id that resolves to nothing (deleted model, someone else's row — RLS makes those identical, which is the point).
  - **UI** (`components/settings/`, `app/projects/[projectId]/settings/`): `AiModelsCard` is structurally `writing-style-card.tsx` — same empty-state-CTA-becomes-header-"+", same append-from-the-action's-return-value with no re-fetch, same optimistic delete. `InstructionsCard` is reused as-is (despite the name it's a generic card shell). `AddModelModal` is two stages in one dialog — key first, model list second — because validating the key is what produces the list. `ModelCombobox` is topic-picker.tsx's combobox, single-select with no "add" row, since the catalog runs to hundreds of entries. **No Figma export exists for this page** (checked all 50 in design-sync/), so the layout borrows the Instructions page's rhythm — restyle when the frame lands.
  - `generate-card.tsx` merges user models into the pill by fetching with the *browser* Supabase client (its page is a client component and can't fetch server-side; queries.ts explicitly accepts either client). Also fixed a real latent crash there: `MODEL_OPTIONS.find(...)!` non-null-asserted on a value restored from localStorage without validation — a model deleted since the last visit would have thrown on render. Now falls back to the built-in, plus an effect that repairs the persisted value, gated on an explicit `userModelsLoaded` flag rather than "the list is empty" (deleting your only model produces an empty list that looks identical to not having fetched yet).
- `components/ui/toast.tsx` rebuilt against the Figma "ToastWithExtraInfo" export: the toast is now a stadium (`rad-rd` → `rounded-full`) instead of `rad-xmd`, and it is **the one place in the app that deliberately skips the squircle clip-path** — a fully-rounded shape has no straight edge for Figma's corner smoothing to blend into, and figma-squircle's path math expects a radius that fits inside the box, so `useSquircleClipPath` is gone from this file (the hook itself is untouched and still the rule everywhere else). New optional `extraInfo` prop renders a white `surface-4` capsule tucked under the toast (the export's −2px gap = `-mt-dist-xs`), styled by the component (radius/padding/`body-sm`/`text-bold`/12px icons) with the caller supplying only content — e.g. a spinner + "Trying to reconnect" under "Network disconnected". Two ordering details: the drop-shadow moved off the presence wrapper onto each capsule individually (matching the export, which gives both their own), and because a `drop-shadow` filter makes each one a stacking context painted in DOM order, the toast needs an explicit `relative z-10` to paint over the capsule pulled up beneath it. Verified against the export in-browser — 32px toast / 20px capsule / 2px overlap / `#fffffe` / 10px-12px-w500 text, all exact.
- Toast entrance reworked from "slide the full height of the element" to a pop: a small px slide (10px, was `translateY(±100%)`), a scale-up from 0.9, and a spring that overshoots slightly before settling. Three consequences worth knowing. (1) **The presence machinery is now `AnimatePresence`** (`motion/react`, already a dep via animated-text.tsx) instead of this file's old `@starting-style`-in / class-flip-plus-`EXIT_DURATION`-timer-out pair — a spring has no fixed duration, so there was no constant left to time that unmount against. The public props are unchanged, so all seven call sites were untouched. (2) **The values were tuned live on a DialKit panel, then frozen** into the `ENTER`/`EXIT`/`EXTRA_INFO` constants at the top of the file once the feel was right — the panel, its handles, and the `toMotionTransition` adapter are gone (git history has them if the entrance ever needs re-tuning). Opacity is pinned to its own ease-out tween rather than riding the spring, since a bouncing fade overshoots past 1, clamps, and reads as a flicker. Exit deliberately defaults to easing, not a spring: overshoot on something that's leaving pulls the eye back to it. (3) The values themselves: enter `slide 15 / scale 0.8 / blur 2 / fade 0.15` on a `bounce 0.6, visualDuration 0.25` spring; exit `slide 16 / scale 0.7 / blur 5` on a 0.2s strong-ease-out. That bounce is well past STANDARDS.md's "keep it 0.1-0.3" — a deliberate call, since a pronounced pop is the point. Checked against Motion's own `spring()` generator in Node: it carries ~3.8px past the resting position and peaks at scale 1.05 around 164ms before easing back. `visualDuration` is time-to-target rather than total settle, so it reads as 250ms even though the tail runs to ~480ms. Motion's `x`/`y`/`scale` shorthands aren't hardware-accelerated (STANDARDS.md), accepted here because a spring interpolates numbers and can't run off a transform string — one small element, animating occasionally.
- The `extraInfo` capsule animates on its own clock rather than riding the toast wrapper: the toast lands, then the capsule slides down and blurs in underneath. Values: `delay 0.4s`, `slide 30px`, `scale 0.5 → 1`, `blur 6px → 0`, `fade 0.2s`, on its own gentler spring (`bounce 0.4, visualDuration 0.3` vs the toast's 0.6/0.25) — it carries ~2.8px past its resting position and peaks at scale 1.047 around 226ms in, so the capsule is fully settled ~0.83s after the toast starts. Its slide is always downward, unlike the toast's own direction-aware one: the capsule starts tucked further up behind the toast and drops into place, so it reads as emerging from underneath it — a direction-aware offset would send it sideways or upward, away from the toast it's annotating. Two things that are easy to get wrong here. (1) **The capsule is two elements, and has to be**: `blur()` and `drop-shadow()` are both the `filter` property, so animating blur inline on the capsule itself replaces its Figma shadow outright — and *leaves* it replaced, since Motion parks `filter: blur(0px)` on the element when it finishes. The outer `motion.div` animates, the inner div keeps the shadow class; the wrapper's blur passes through to the shadow too, so the whole capsule blurs in as one piece. Same category of fix as the shadow/clip-path split at the top of this component. (2) **A per-property transition override replaces the whole transition for that property**, so the `opacity` override has to repeat `delay` — it does not inherit it from the parent transition object. The capsule deliberately has no `exit` of its own: the toast leaves as one object.
- **Network-failure handling, app-wide.** The root problem was that "the request failed" and "the request was rejected" are indistinguishable at most call sites, and every site was guessing the pessimistic reading: Supabase returns an error from `signInWithPassword` whether the password was wrong or the request never arrived, and `auth.getUser()` yields no user whether the session expired or the auth server was unreachable. So a dropped connection said *"Incorrect email or password"* on the login screen, and bounced people to `/login` mid-session when they hit Generate.
  - `lib/network-error.ts` is the single predicate (`isNetworkError`), used by both server and client — it covers Node's `fetch failed`/undici `cause` chain, the browser's `Failed to fetch`, Supabase's `AuthRetryableFetchError` and its `status: 0`, and explicitly excludes `AbortError` (a deliberate cancellation also surfaces as a rejected fetch). It also exports the `ActionError` shape server actions return: `{ error, network?: true }`, where `network` is what tells the client to raise the toast instead of blaming a field.
  - **Server-side, the two reported bugs.** `lib/supabase/middleware.ts` (reached via `proxy.ts` — Next 16's renamed middleware) no longer redirects to `/login` when `getUser()` failed for network reasons *and* the browser still carries an `sb-…auth-token` cookie: "we couldn't check" is a far likelier story than "they're signed out", and RLS still gates every row, so the worst case is a page with no data rather than one that leaks any. `app/projects/[projectId]/layout.tsx` gets the same guard (safe because `fetchProject` *throws* on a failed query rather than returning null, so this can't render a project page for someone signed out). All five auth actions (login, signup, verify-signup, password reset request/verify/update) now check `isNetworkError` *before* their "wrong credential" branch. `post-actions.ts`'s `requireUser` returns *which kind* of no-user it saw, so generation reports `network` instead of `not_signed_in`.
  - **Client-side, and this is the standard: `withNetworkStatus` at every call site.** `lib/network-status.ts` holds a module-level store (not context — the things that report a network problem are mostly event handlers and action call sites, not components) and exports `withNetworkStatus(promise)`, which is how a client component should call *any* server action. It returns the result, or `null` when the request never landed — having already raised the toast — and rethrows everything else untouched so redirects (server actions signal those by throwing) and real bugs still propagate. Call sites read `if (result === null) return`. An earlier pass did this by monkey-patching `window.fetch` instead; that was replaced by explicit wrapping on request. Note the patch would not have worked for server actions anyway — React captures its `fetch` reference at module init, so a later override never sees those calls.
  - Every client call site is wrapped: the six auth screens, My-voice autosave, both instruction modals and both entry blur-saves, all three optimistic deletes, create-project, add-model (both stages), the generation loop and its four `updatePost` sites, and generate-card's browser-side `fetchUserAiModels`. Optimistic deletes undo themselves on `null` but stay quiet — the disconnected toast already explains it, and a second "couldn't delete that item" toast would just be noise.
  - `components/shared/network-status.tsx` mounts once in the root layout, subscribes via `useSyncExternalStore`, and renders the danger toast ("You're offline", extra info "Check your connection", no spinner; `z-100` so connectivity outranks whatever else is being reported). `navigator.onLine` is used only to raise the flag, never to clear it — `false` definitely means disconnected, `true` only means an interface is up; clearing is left to a real request succeeding. While offline it re-probes every 5s so the toast clears itself shortly after the connection returns.
  - **The probe must target the backend, not the app's own origin.** It first hit `/favicon.ico`, which in development is localhost — reachable with the wifi switched off — so the toast dismissed itself about 5 seconds after appearing. It now probes `${NEXT_PUBLIC_SUPABASE_URL}/auth/v1/health` with `mode: "no-cors"`, which makes the result binary in the way this needs: a reachable server resolves (as an opaque response nothing reads) and only a real connectivity failure rejects, so a CORS or 4xx quirk can't masquerade as being offline. For the same reason `withNetworkStatus` no longer clears the flag on a successful action — our own server answering proves nothing on localhost, and if the action's own result then reported a server-side network failure the toast would flicker off and straight back on. `probeNetwork` is the only thing that clears it; a success just triggers a probe, and only when the flag is actually set. Verified with real CDP offline emulation: the toast holds at full opacity across four probe cycles while offline, and clears the instant the connection returns.
  - **Server-rendered navigations** — where a Supabase outage makes a *server component* throw mid-render, out of reach of anything client-side — land on `error.tsx`. Three of them, all rendering `components/shared/route-error-recovery.tsx`: `app/error.tsx` (catch-all), `app/projects/error.tsx`, and `app/projects/[projectId]/error.tsx`. **Placement is load-bearing: a layout's own errors are caught by the boundary one segment up**, so `[projectId]/layout.tsx`'s `fetchProject` throwing is caught by `app/projects/error.tsx`, not by the sibling inside it. The recovery screen can't read *why* it was rendered — Next redacts server-side error messages in production, leaving only a digest, so `isNetworkError(error)` is useless there. The copy therefore doesn't commit to a cause — caption "Something's wrong" over "We couldn't load this page. Check your internet connection & try again." per the Empty state export (which drops the earlier "You might be offline" line) — and the probe is used only to drive recovery, not wording: while unreachable it re-probes every 5s and retries on its own the moment the connection returns. **`reset()` alone is not enough and looks like a dead button**: it re-renders the boundary against the same already-failed RSC payload, so the retry is `router.refresh()` (re-fetches from the server) *then* `reset()`, both inside a transition that drives the button's pending state. Verified by making a page throw for a fixed window and confirming the button brings the real page back once it stops.
  - **Two things this needed from the toast:** `duration` accepts `null` now (no auto-dismiss — this toast reports an ongoing condition, not an event), and the auth screens call `reportNetworkIssue()` and `return` early rather than calling `setError` on a field, since a connectivity failure isn't the fault of anything the user typed.
  - `lib/network-error.test.ts` and `lib/network-status.test.ts` pin the classifier in both directions (a wrong password and a 500 must *not* read as network failures; an `AbortError` must not either) and `withNetworkStatus`'s contract including the rethrow.
- **DialKit's default panel id is per component instance, not per panel name** — `panelId = options.id ?? \`${name}-${useId()}\`` (node_modules/dialkit/dist/index.js, `useDialStorePanel`). So a `useDialKit` call inside a *reusable component* registers a fresh duplicate panel for every mounted instance: the Instructions page showed four identical "Toast" panels (My voice, Writing style, References, plus the preview button) before this was caught. The fix at the time was an explicit stable id (`{ id: "toast" }`), which collapses them to one shared set of controls and as a side effect opts into DialKit's `retainOnUnmount`. The toast panels are gone now, but the trap isn't: `useShake` (hooks/use-shake.ts) has the same latent duplication and just hasn't bitten yet, because only one shaking element is ever mounted at a time. Rule of thumb: a `useDialKit` call anywhere other than a single-instance component needs an explicit `id`.
- Automation note for anyone verifying animations in-browser here: **every `javascript_tool` eval backgrounds the tab** (`document.visibilityState === "hidden"` inside the eval), which suspends rAF — so Motion's animations freeze at their initial frame and `setInterval` throttles to ~1/sec. Mid-flight DOM sampling of a JS-driven animation is therefore not possible through that path; it only ever reports the `initial` state. What does work: reading initial/settled values via eval, catching frames via `computer`'s screenshot/zoom (those foreground the tab), and — for spring math specifically — running Motion's `spring()` generator in Node and inspecting the trajectory directly.
- `components/shared/empty-state.tsx` — the app's one blank-page layout, from the Figma "Empty state" export (design-sync/empty-state): a 48px fill-weight icon in `icon-minimal` (the export's own #cac2bf), a `body-md`/`text-subtle` caption, then a `heading-sm`/Phudu title with an optional action, `gap-dist-lg` between the three and `gap-dist-xl` between title and button. **The caption/title hierarchy is inverted on purpose** — the small grey line names the state, the big line carries the message — so adopting it means resplitting copy that used to be "small heading over long body paragraph", which is what the dashboard's did. Both text blocks are a fixed `w-68` (272px) rather than fluid, which is what produces the title's deliberate 3-4 line wrap; the action **hugs its content** (no `w-full`, unlike buttons inside cards and forms). Figma's "Size=md" button maps to the existing `size="xl"` — 40px tall, `rad-lg`, `pad-lg` sides, `btn-lg` text — so no new Button size was needed. Used by `route-error-recovery.tsx` (WifiSlash) and the dashboard (Eyes, the default icon); verified against the export in-browser — 48px icon, 16/24px gaps, 272px text blocks, 27/32 Phudu, 40px hugging button, all exact.
- **The in-project page spinner now appears the instant a tab is tapped**, instead of whenever the router got round to committing the navigation. `loading.tsx` alone can't do this: that boundary only renders once the router *commits*, which waits on the server, so a slow section left the previous page on screen with no spinner for the better part of a second — defeating the point of the loading state. `lib/section-navigation.ts` is a module-level store (same pattern as lib/network-status.ts, so no provider had to be threaded through the layout): each sidebar `<Link>`'s own `onClick` reports the destination href, and `components/shared/section-content.tsx` subscribes and wraps the page area.
  - **`useLinkStatus` is deliberately not used, and this was the bug.** The first version of this wired the store to that hook from a `SectionNavigationReporter` rendered inside each `<Link>`; both ends of it are wrong here, per Next's own docs (`node_modules/next/dist/docs/01-app/03-api-reference/04-functions/use-link-status.md`). Its pending flag is *"true before history updates, false after"* — the URL changing, not the new tree painting — so the overlay could lift while the outgoing page was still mounted underneath it, which is exactly the flash of the previous section that kept getting reported. And it is *skipped entirely for a route that has already been prefetched*, which every sidebar link is, so in the common warm case it never fired at all. That doc's own "You might not need useLinkStatus" section names both conditions this app meets (prefetched destination, `loading.js` present).
  - **Both ends are now things that cannot happen early.** The start is the click itself (`onClick`, before the router has done anything, prefetched or not). The end is `usePathname()` reaching the stored href — i.e. the router commit that renders the new page — so the overlay can only ever hand over to the new content or to loading.tsx's identical spinner. Verified by MutationObserver: overlay up while still on `/dashboard` → pathname flips to `/instructions` **with the overlay still up** → instructions content renders **still underneath it** → overlay lifts 176ms later. Same ordering in the reverse (warm) direction, where the new content and the pathname arrive in one commit and the overlay lifts 8ms after.
  - The store holds the **latest href** rather than a count: tapping a second tab mid-navigation means the destination *changed*, so the overlay should stay up until that second route lands, not drop when the first one does. Modified clicks (cmd/ctrl/shift/alt, middle button) are skipped — they open the section elsewhere and never navigate this document, so they must not arm an overlay only arrival here can clear. Two escape hatches for the same reason: `MAX_VISIBLE_MS` 10s, and a clear on `SectionContent`'s unmount (leaving the project mid-navigation would otherwise strand the href and greet the next project with an unsatisfiable overlay).
  - **Tapping the tab you're already on is a no-op** (`event.preventDefault()`), since re-running the whole navigation — refetch, spinner, scroll reset — over content already on screen reads as the page breaking. The check is exact (`pathname === href`), unlike the `active` highlight's `startsWith`: on a sub-route like `generate/generating` the Generate tab still highlights, but tapping it has somewhere real to go. Verified: clicking the active tab produces zero DOM mutations in `<main>` and zero navigation fetches.
  - It's an *overlay* (absolute, `bg-surface-3`) rather than swapping `children` out, so the outgoing page keeps its DOM and scroll position until the new one is ready. `loading.tsx` stays for the navigations that never go through a tab (direct URL, deep link, back/forward) and both render the same `components/shared/section-spinner.tsx`, so the overlay handing over to the boundary mid-navigation is invisible.
  - **It is a hard cut, not a cross-fade, and that's the fix for a reported flicker** — "sometimes it flashes the previous page before showing the current one, sometimes it flashes the current one". Both symptoms were the same cause: a fade leaves a window where two layers are partly visible, and the layer *underneath* changes identity during it. Fading out over children that hadn't swapped yet showed the old page; fading in or out around a fast commit showed the incoming one. Opacity is now only ever 0 or 1.
  - Two timers replace the old CSS delay and are what keep a hard cut from flashing: `SHOW_DELAY_MS` 120 (a warm, prefetched section resolves inside this and never shows a spinner at all) and `MIN_VISIBLE_MS` 300 (once shown it stays, so a navigation finishing just past the delay can't put a spinner up for two frames and rip it away). The cost is bounded and only lands on navigations in the ~120-420ms band; outside it there's no added latency. Verified by MutationObserver over three navigations: cold → overlay mounted 606ms, route committed 796ms *with the overlay still up*, unmounted 1602ms — i.e. the new content was always already in place before the overlay lifted; both warm navigations never mounted it at all.
  - **The router will commit a *superseded* navigation's late-arriving content, and that — not the overlay's own machinery — was the remaining "previous page flickers" report.** Reproduced by MutationObserver: tap Instructions, tap another tab while its loading fallback is still up, and the abandoned instructions content paints *in full* the moment its response lands — before the new route commits anything — for 17-425ms across runs (nondeterministic: sometimes the router discards it instead, hence "sometimes"). `location.pathname` still shows the old route during the flash, so `pending` is true and a *visible* overlay covers it — the only uncovered window was `SHOW_DELAY_MS` itself. So a navigation armed while another is in flight skips the delay: `startSectionNavigation` stamps `instant: true` when the previous target never arrived **or** the route's loading fallback is currently mounted (loading.tsx is now a client component purely to report its own mounted window to the store), and `SectionContent` renders the overlay for an instant navigation straight from the arming render — synchronously inside the click, because the stale commit arrives on a network task that can beat a passive effect; `visible` catches up via a 0ms timeout so the minimum-visible clock still runs. The delay's only job is keeping warm settled-page taps spinner-free, and a mid-stream tap is never that (a spinner is already on screen, and the overlay's spinner is the same one in the same place). Re-verified: the same stale instructions commit now paints with `ov:true` in the log, overlay armed 40-66ms after the mid-stream click with throttled timers (i.e. via the render path, not a timer), and a filter for "stale content visible without overlay" over the whole run comes back empty. `lib/section-navigation.test.ts` pins the `instant` contract.
  - The spinner inside the overlay is `sticky top-1/2`, not centred in the overlay itself: `<main>` is the scroll container, so on a long page (Instructions) an overlay centred across its full height would put the spinner off-screen.
  - Measure this with a MutationObserver on `<main>`, never polling — every `javascript_tool` eval backgrounds the tab, which clamps `setTimeout` to ~1s and makes absolute timings meaningless. The *ordering* in the observer log is what's trustworthy, and ordering is the whole property being checked.
  - A first attempt put the spinner on the sidebar tab icons instead. That was a misread of the ask — reverted; the tabs keep their own icons and the optimistic `pendingPath` highlight, nothing more.
- DialKit is now gone from `hooks/use-shake.ts` too (its "Error shake" panel was the one showing on the calendar-based Generate page) — values frozen as `SHAKE_DURATION_MS 480 / SHAKE_AMPLITUDE_PX 10 / SHAKE_DECAY 0.7 / SHAKE_HOPS 5 / ease-out`, and the effect's dependency array collapses to just `[trigger]`. The two panels in `generating-view.tsx` ("Generating heading (elastic)", "Generating card") are still live — they belong to the /generating route, not the Generate page.
- **The generated card's Regenerate button runs a real generation** — it used to be a 1200ms `setTimeout` that showed the placeholder and put the same text back. New `regeneratePost` server action (post-actions.ts) rerolls one post's content in place, through the same prompt builder as the batch. Three things shape it. (1) **It's an UPDATE, not a delete-and-insert**, so the post keeps its id (the card is keyed on it), its scheduled date and its platform — nothing about where it sits in the batch moves because its text was rerolled. Confirmed against the DB: a run plus two regenerations leaves exactly one row. (2) **Everything the prompt needs is read from the stored row, not sent by the client** — platform and topic can both have been changed on the card since it was generated (the social pill, the date actions), and the row is the only thing that knows the current values; the client only says which post and which model. RLS makes someone else's post id read as a post that doesn't exist. (3) `buildPostPrompt` gained a `previousContent` option: the brief is byte-for-byte the one that produced the post, plus a final section quoting the rejected text and asking for a new angle/opening/structure. It goes *last*, after the "write one post" line, so the constraint the user just asked for is the most recent thing the model reads — and it's appended in both the structured and single-prompt branches. Without it a reroll is just the same prompt at temperature, which lands near the same answer.
  - The batch-context cache and the BYOK-fallback check were inlined in `generateAndSavePost`; both are now `resolveGenerationContext` / `runGeneration`, shared by the two actions unchanged (a regeneration is the same brief, so it resolves and reuses `batchContextId` exactly the same way, and refreshes the ref from its own response).
  - Client side: `GeneratedPostCard` takes `onRegenerate: () => Promise<void>` and holds the `GeneratingPostCard` placeholder for exactly as long as that promise is in flight — no timer, so a slow model keeps it up and a fast one drops it the moment new text lands. `GeneratingView.handleRegeneratePost` is the one handler here that is **awaited rather than optimistic** (every other one — delete, date, platform, edit — writes through immediately and undoes on failure): there is no new content to show until the model has produced it. A failure leaves the original text in place, since it's still the truth, and reports via the existing Toast / `reportNetworkIssue` split.
  - TasteTest gets `pickDifferentTasteTestContent(current)` — the by-index pick would hand back the exact string being replaced, so it's random, stepped forward once if it collides.
- **Content section built** (route path stays `calendar`) from the Figma "Content / Empty state" + "Content / Base calendar view" exports: three tabs over month sections, each a surface-3 tray of day chips ("🗓 12 / 5 posts").
  - **Tab membership is decided by date, not by the posts table's `status` column** (per direct instruction): Queued = scheduled in the future, Published = scheduled in the past, Draft = no date at all. `lib/content-grouping.ts` owns all of it (`postsForTab` / `groupPostsByMonth`, pinned by lib/content-grouping.test.ts). Queued reads forwards (next thing out on top); Published and Draft are newest-first, months and days both.
  - **Drafts have no date, so they group by their created date** — a decision, not an inference: it keeps the chips and the "tap a day" interaction identical on all three tabs instead of inventing a dateless chip shape. `groupingDate()` is the single place that switches.
  - Grouping reads **local** date parts on purpose: a scheduled day is picked in the user's timezone (Calendar hands back local midnight, stored as UTC), so only a local read recovers the day actually chosen. The client render is the authority; a server rendering in another timezone is approximate for posts within a few hours of midnight. Relatedly, `now` is stamped once by the page and passed in as a prop so the future/past split can't differ between the server render and hydration — that's the one `eslint-disable react-hooks/purity` in the file, explained inline.
  - **The panel is now shared**: `components/shared/glow-panel.tsx` (green pixel-glow art, squircle, the corner info marker, `data-clip-boundary`) — both exports draw it identically, so `GeneratePanel` became a thin wrapper that only adds its own reset-calendar button via a new `cornerAction` prop. No visual change to Generate; verified in-browser.
  - **GlowPanel gained `min-h-fit`, and that's load-bearing**: `overflow-hidden` (needed to crop the oversized glow art) zeroes the element's automatic minimum size, so as a `flex-1` child of the layout's scrolling `<main>` the panel was pinned to the viewport's leftover height and silently clipped anything past it — fine for Generate, wrong for a page with a section per month. `flex-1` still fills the space when content is short, which is what centres the empty state. Verified by injecting a 1500px probe: panel grows 1018→1908px, `<main>` becomes scrollable, probe stays inside the panel's bounds.
  - `DayChip` uses `min-w-22` rather than the export's fixed 88px — every chip the export draws holds a single-digit count; a real "23 posts" wrapped onto two lines inside the fixed width. Hugging from 88px up keeps every case the export shows pixel-identical.
  - **"Show as" cycles the layout, and the choice sticks.** Each tap advances through `CONTENT_VIEWS` (lib/content-view.ts — **Kanban first, which is what makes it the default**; a List view has been mentioned but not exported, and adding it is one array entry). The pick is remembered per project, matching how the Generate page remembers its own form. It's read through **`useSyncExternalStore`, not an effect that copies localStorage into state**: localStorage doesn't exist on the server, so this is exactly the "external store with a server snapshot" case that hook exists for — and the obvious alternative trips `react-hooks/set-state-in-effect`. Same shape as lib/network-status.ts and lib/section-navigation.ts. There's no local copy of the view at all: `setContentView` writes and notifies, and that's what re-renders. The icon's rotation stays local state, since the spin belongs to the act of switching rather than to the state — a restored view starts unrotated. One visible consequence: a project saved as Calendar renders Kanban for the first frame, then switches, because the stored value can't be known until hydration. It takes the app's standard 150ms press scale, and the ArrowsClockwise icon turns **half a rotation per tap, accumulating** rather than resetting — repeated taps keep spinning the same way instead of snapping back, which is the point: the icon is the act of switching, not a state readout.
    - **That turn is a WAAPI animation, not a CSS transition, and the difference is load-bearing.** As a transition it sometimes skipped and the icon jumped — reproduced in-browser: same DOM node, `transition-property: rotate` and a 0.3s duration both applying, the value going 720°→900°, and **no `transitionrun` event at all**. A transition only starts if the browser observed the old value in a rendered frame first, and the tap that changes it also swaps out every month underneath — a commit big enough that the change could land without one. An animation states its own from/to, so it can't depend on what the previous frame happened to compute. The inline `rotate` still carries the resting value for the animation to land on (verified: taps produce 0→180 then 180→360, 300ms each).
  - **Kanban view** (design-sync/content-kanban-view), sharing the page's chrome and the same month grouping — a `MonthBoard` per month instead of a `MonthSection`, holding one `KanbanColumn` per day that has posts. The board row is a fixed 256px tall with every column filling it, per the export, and scrolls sideways by wheel or drag (`useDragScroll`, same gesture as the day deck and the skip-dates carousel). **A column whose posts overflow scrolls its own stack** — the export simply clips there, having no way to show a scroll, but leaving posts unreachable isn't an option in the real thing.
  - `KanbanPostCard` is the compact card that stacks in a column: same surface, radius, padding, platform pill and topic chips as `GeneratedPostCard`, two lines of content then an ellipsis, and **no border** (it sits on the column's own surface-3 tray, which is what separates it from its neighbours). Deliberately its own component rather than a variant of that one despite the design lineage — it shares no behaviour with it at all (no inline editing, no date picker, no regenerate, no actions menu) and the whole card is a single control. It's already a `<button>` with press feedback but no handler: it opens the post's own page once that exists.
  - The "Tap on a day to view its content" line is gone (direct request), so the tabs sit straight above the months.
- Kanban follow-ups, per direct feedback:
  - **`hooks/use-scroll-fade.ts` — the app's scroll-aware edge fade**, now shared by the board row (horizontal), each column's stack (vertical) and each card's pill/topics row (horizontal). Each edge's fade is the *actual remaining scroll distance there*, capped at the configured width, not a binary on/off — which is what makes it usable at rest: an unscrolled element has nothing behind its start edge, so that fade is zero and resting content is never dimmed. That removes the padding/negative-margin dance the **static** masks elsewhere need (GeneratedPostCard's topics row, the skip-dates carousel) to keep the fade off resting content. It writes the mask straight to the element instead of returning a style: this runs on every scroll frame, and routing it through React state would re-render the subtree each time. Its `useLayoutEffect` deliberately has no dependency array — a ResizeObserver only fires when the element's own box changes, and these are fixed-size scrollers, so adding or removing content changes what's scrollable without resizing anything.
  - Board height 256 → **400px** (`h-100`), three cards to a column instead of two — but only when the month needs it: **at two posts or fewer in its busiest day the board hugs its content instead**, so a row of one- and two-card columns doesn't leave a band of empty tray under every one of them. The busiest day decides for the whole month, since columns in a row all share the tallest one's height (flex stretch). Leaving the height class off is the entire mechanism: the row goes auto-height and `h-full` on a column inside it simply resolves to auto. Verified across a real month set — tallest 3 → 400px fixed, tallest 2 → 292px, tallest 1 → 168px.
  - The topics row on a Kanban card scrolls instead of clipping, and stops pointer events from reaching the board so a drag over it scrolls the row rather than both.
  - The page's own scroll area (the months, in either layout) carries the same fade — one `useScrollFade` in ContentView whose callback ref follows whichever of the two layouts is mounted.
  - **The Content page is now exactly the viewport's height and scrolls its own months**, rather than growing and taking the page's scroll with it. `GlowPanel` lost the `min-h-fit` it was given earlier (it exists to fill the viewport, not to outgrow it), and `calendar/page.tsx` wraps it in `relative flex-1` + `absolute inset-0`. **That absolutely-positioned child is the whole trick**: every wrapper between `<main>` and the panel sizes itself from its content, so a tall list simply pushed them all taller — and an out-of-flow child contributes nothing to its parent's content-based minimum, so that parent collapses to what `flex-1` gives it and `inset-0` hands the panel a definite height to divide up. Done in the page rather than by capping the shared `SectionContent` wrapper, which every other section relies on being able to outgrow (they page-scroll, and its loading overlay is sized against it). Verified: `main.scrollHeight === clientHeight` (no page scroll) with the month list scrolling internally at 940 vs 798.
- The deck card's actions menu gained an **"Open up"** row (ArrowsOutSimple) that routes to the post's own page. `PostActionsMenu`/`GeneratedPostCard` take it as an optional `onOpen`, so the row simply isn't rendered where there's nowhere to send the post — the Generating page passes nothing.
- **Post details page** (`calendar/[postId]`, components/content/post-details.tsx) from the Figma "Content / Post details" exports — the two are one screen with two differences: the heading is the scheduled date or the word "Draft", and the middle action moves the post the other way (a dated post back to drafts with the Scribble icon, a draft onto the calendar with CalendarDots). **UI only, by request**: the three actions and the edit pencil render but do nothing; Back is wired, since a page with no way out isn't a screen you can look at. Nested under `calendar` so the sidebar keeps Content highlighted (it matches on startsWith) and the section-navigation overlay treats it as the same section. A Kanban card is now a `<Link>` to it rather than a button — cmd-click and middle-click behave, and a drag across the board still can't trigger it because the drag hook takes pointer capture past its 4px threshold. The drafts export's middle button still carries the "Move to drafts" *label* from the other screen; the icon is the tell, and it matches what that action does everywhere else. `GlowPanel` gained `showInfoMarker` (default on) — this screen's own actions occupy that corner, and the export shows them there alone.
  - **`GlowPanel` clips with `overflow-clip` *and* an explicit `min-h-0`, and it needs both.** `hidden` still makes an element a scroll container, and the glow art is deliberately wider than the panel (163px, split either side), so the panel had a scrollable overflow nothing was meant to reach — anything that scrolls an element into view could shove the entire page content sideways and leave it there, which is what happened after an accessibility-tree click (panel at `scrollLeft: 163`, title off-screen). `clip` crops identically and is never scrollable (verified: `scrollLeft` stays 0 when set to 300).
    - **But `clip` does *not* zero a flex item's automatic minimum size the way `hidden` does** — an earlier note here claimed it did, and that was wrong. Switching to `clip` on its own silently un-capped the panel: it grew to its full content height (2000px inside the 1018px box meant to cap it) and the whole page went back to scrolling, mask and all. `min-h-0` says it explicitly rather than relying on either overflow value to imply it. Verified after the fix: `main.scrollHeight === clientHeight` (no page scroll), panel 1018 within the viewport, months list scrolling internally at 1808 vs 826, and the fade back — no top fade at rest, 24px once scrolled, 48px at the bottom throughout.
  - A hook returning `{ ref, … }` must be **destructured at the call site** (`const { ref: rowRef } = useScrollFade(…)`); reading `scrollFade.ref` straight into JSX trips `react-hooks/refs` ("Cannot access refs during render"), which is why every other hook here is consumed that way. The Published/Draft empty-state copy is written to match the exported Queued pair, which is the only one that exists. The export's "view it's content" typo is corrected to "its".
- **Day deck** (components/content/day-deck.tsx), from the Figma "Content / Day clicked" export: clicking a day chip opens that day's posts as a row of cards over a scrim, and the cards fan out of the chip like a deck being opened.
  - **It's an overlay, not a route** (the export shows the Content page blurred behind it), and it is **portalled to `<body>` — which is load-bearing**: GlowPanel carries a clip-path, and any clip-path other than `none` makes that element the containing block for its fixed-position descendants, so rendered in place a `fixed inset-0` overlay laid itself out *inside the panel* and was then cropped by the panel's own `overflow-hidden`. The scrim only dimmed the panel and the first card was sliced off. Same reason the app's dialogs go through Base UI's Portal.
  - **The card is `GeneratedPostCard`, unchanged** — the export's "Post / Type=calendar" is that component down to the 4px border-subtle stroke, rad-lg, pad-lg and dist-md gap. It gained an optional `className` (the deck renders it at the export's `w-68 h-98`, not the Generate grid's `h-92 w-full min-w-70`, which is pinned to GeneratingPostCard) and defaults for its six DialKit props, so a call site with no dials doesn't invent six numbers.
  - **The animation is WAAPI, not Motion or CSS classes.** Each card's start state is computed per card at the moment it animates — `translate` to put its centre on the chip's centre, `scale` = chip width / card width (0.32 here: it genuinely starts at the size of the thing it came out of, never `scale(0)`), and a ±3°-per-step tilt capped at 9° so the stack reads as a squared-up deck rather than a scatter. Measuring at animation time (not open time) keeps it correct after the row has been scrolled. WAAPI because it's predetermined motion over a dozen-plus elements at once — STANDARDS.md's own recommendation, hardware-accelerated via a full transform string, no library, and the same tool `useFlipReorder` already uses. Values: 420ms open / 260ms close (asymmetric — closing is the system responding), the strong `cubic-bezier(0.23,1,0.32,1)` ease-out both ways (never ease-in), 45ms stagger **capped at 270ms total** since a 20-post day at 45ms each would run past a second.
  - **Stagger runs outward from the middle**, so the deck opens as a fan rather than dealing left-to-right; closing reverses it, flanks first. `stepsFromMiddle` uses `Math.floor`, not `Math.round` — an even-numbered deck has no single middle card, and rounding put its innermost *pair* on step 1, delaying the whole deck by one beat before anything moved (verified in-browser: delays now 0/0/45/45 for four cards, and the four start transforms land their centres on exactly the chip's centre).
  - Closing mid-open cancels the running open animation before measuring, which snaps that card to its resting slot for a frame — only reachable inside the first ~half second, and the alternative (measuring a rect that's still moving) sends it somewhere wrong entirely.
  - Scrim and close button ride **`@starting-style` in, a class flip out**. An rAF-driven flip was tried first and is wrong here: rAF never fires in a backgrounded tab, so a deck opened in one stayed scrim-less. Unmount waits on `Promise.all(animation.finished)` with a `setTimeout` backstop, the same suspended-tab trap the skip-dates carousel's exit hit.
  - **A too-wide deck opens centred on its middle, and that's a scroll position, not a layout property** — auto margins collapse to 0 once the row overflows, so `mx-auto` alone left it pinned to its first card. Set before the cards are measured, so they fan out to where they'll actually rest. Verified with a real 23-post day: `scrollLeft` lands exactly on centre and the middle card's centre is the viewport's.
  - Every card action is wired to the existing server actions (updatePost/deletePost/regeneratePost) with the app's standard optimistic-then-undo shape; the deck writes through an `onPostsChange` updater the page owns, so a post whose date moves off this day leaves the deck *and* updates the chips behind it in the same beat. Regenerate has no model picker here, so it reads the one the Generate page persisted (`lib/generate-settings.ts` — a BYOK user's reroll should stay on their own key) and falls back to the built-in.
  - Escape, scrim click and the Close button all dismiss; focus moves to the overlay on open and returns to the chip on close. Emptying a day from inside the deck unmounts it without the put-away animation, deliberately — playing it would mean holding a deck of nothing on screen while it ran.
- Follow-up round on the deck, per direct feedback:
  - **Dates carry an ordinal suffix** — `lib/format-date.ts` (`formatOrdinal` / `formatFullDate`), used by the day chips ("5th"), the post cards ("July 5th, 2026") and the deck's accessible name. `GeneratedPostCard`'s own `formatDate` now points at it, so the Generate page reads the same way; `Intl` has no ordinal-day format, hence the hand-built string (month and year still come from `toLocaleDateString`).
  - **Drag-to-scroll is now shared**: `hooks/use-drag-scroll.ts` holds the 4px threshold, pointer capture and resisted edge overshoot that the skip-dates carousel had inline; `useCarouselScroll` keeps only its own `data-clip-boundary` measurement and composes the rest. **The hook deliberately holds no element ref** — its handlers read `event.currentTarget`, because a ref threaded into or out of a custom hook reads to the React Compiler as a ref accessed during the caller's render (`react-hooks/refs`). The deck's row applies `select-none` *only* while a drag is running, so card text stays selectable and double-tap-to-edit keeps working. (generate-calendar-column.tsx still reports that same `refs` error at its `ref={dragScroll.ref}` call site — pre-existing, confirmed against HEAD, untouched by the extraction.)
  - **The deck's motion was tuned on a DialKit panel and is now frozen** — same treatment as toast.tsx's entrance and use-shake.ts; the panel is gone (git history has it if this needs re-tuning), along with the signature-keyed replay effect that only existed so a dial change would re-deal an open deck. Values: enter 400ms/80ms stagger, exit 480ms/80ms, both bounces 0, maxStagger 270, tilt 8. The bounce path stays in `enterKeyframes`/`exitKeyframes` behind a `> 0` guard, so raising either constant puts the overshoot keyframe back. The bounce dials stayed (they're what the overshoot keyframe is built from) but tuned to 0: the deck reads better as a clean fan than a springy one, with the wider stagger carrying the "dealt one after another" feel. The exit running *longer* than the entrance inverts the usual asymmetry deliberately — the cards have further to travel back into a single chip than they did fanning out of it. Bounce is a *scale overshoot*, not a spring: an extra keyframe at 0.72 of the entrance carrying `scale(1 + bounce)` before settling, and at 0.22 of the exit as a wind-up before the card is pulled home. Every keyframe writes the full `translate/scale/rotate` list (a bare `none` would drop the browser into matrix interpolation). Changing an enter dial **replays the entrance on the open deck** (keyed on a signature string, not on the card set — a delete mustn't re-deal), without re-centring the scroll.
  - **The scrim now waits for the cards.** It was fading on its own 200ms clock while a long deck's put-away ran several times that, so the backdrop vanished with cards still flying across a fully lit page. It now holds and starts its fade at `exitTotal - CHROME_MS`, where `exitTotal` is derived from the same dials the cards use. The close also switched from `Promise.all` to **`Promise.allSettled`**: `all` rejects the instant any one animation is cancelled, and that rejection unmounted the whole deck — cutting every *other* card's flight short. `exitTotal` measures the **middle** card, not the first: entering, the flanks wait longest; leaving, they go first and the middle card is last home (`longestDelay`, shared by both). Getting that backwards had the scrim finishing 270ms early on a 23-card deck — verified live: 3 cards → exit delays [0, 45, 0], last card home at 525ms, scrim delay 325ms + 200ms duration = 525ms exactly.
  - **The platform pill and the topics share one row** on `GeneratedPostCard` (per direct request), the pill holding its width and the topics scrolling in what's left. The topics row needs `min-w-0` to shrink at all — a flex item's automatic minimum is its content, which otherwise pushes the pill out of the card. Its edge-fade mask is now asymmetric: 16px right (out to the card's inner edge, as before) but only 8px left, since a 16px fade there would reach back under the pill. Each side is padded by its own fade width and pulled back out by the same margin, so the fade never dims a resting chip.
  - **`GeneratingPostCard` takes a `className` too, and `GeneratedPostCard` forwards its own to it.** The placeholder matches the real card's default footprint on its own, but a caller that resizes the card (the deck's `h-98 min-w-0` inside a `w-68` wrapper) has to resize both — otherwise a card visibly changed size the moment it stopped being a placeholder. Verified: 272×392 for both in the deck, unchanged `h-92 w-full min-w-70` everywhere else.
  - **Edits that take a post off the open day animate it home first.** Change date (to another day), turn to draft and delete all route through `leaveDeck`, which plays the same put-away one card gets during a full close and only then hands the change to the caller. **Holding the change back is what makes the animation possible at all**: the caller owns the post list, so the instant it stops including this post the card unmounts and there's nothing left to animate — the deck therefore keeps the change in `pendingCommits` until the animation resolves (or the close flushes them). A failed server call runs `cancelLeave`, which drops the pending change and cancels the animation — the card snaps back rather than flying, a rare enough path not to warrant a second animation. This is the one place the app's optimistic-delete convention bends: the card still leaves immediately, it just leaves by flying home.
  - **The last card takes the deck with it** — `leaveDeck` calls `startClose()` instead when there's only one post, so the card and the scrim go together rather than leaving an empty overlay standing. Verified both paths in-browser: on a 3-post day the card flew home, the deck stayed open and the chips behind went 3→2 and 1→2; on a 1-post day the same edit closed the overlay and the day's chip disappeared.
  - **The remaining cards close the gap** once one has flown home — `useFlipReorder`, the same quick-move curve as the Generate page's grid, since a reflow isn't something CSS can transition on its own. `REORDER_DURATION_MS` went 150→**100ms** on direct feedback ("takes too long"): a gap closing behind a removed item is a consequence, not an event, so it only has to stay legible as movement rather than a jump. The constant is shared, so the carousel and the Generate grid got quicker with it. **The elastic-drag transform had to move off the row for this to work**: a transformed element becomes its descendants' offsetParent, and with the transform on the row itself every card's offsetLeft was measured *inside* the row — so cards after a removal showed a delta but cards before it read zero, even though the row is `mx-auto` and re-centres, visibly shifting them. The transform now sits on a wrapper around the row, so each card's measurement includes the row's own re-centring. Verified by hiding a middle card and re-measuring: the two survivors read −144 and +144 (both animate) where the first previously read 0. Long decks still scroll identically (23 cards, scrollWidth 6672 vs clientWidth 2056, last card flush at max scroll).
  - The card wrappers carry `data-post-id` and `orderedCards()` reads them out of the DOM rather than keeping a ref map: the wrappers already hold the FLIP hook's ref, and composing a second one meant caching a callback per id, which is a ref read during render (`react-hooks/refs`).
  - `dayKeyForPost` (lib/content-grouping.ts) is the single predicate behind that: it answers "which day group does this post land in, or none at all" for a given tab, so the deck can tell a date change that keeps a card here (same day, different time) from one that moves it to another day, another tab, or off every tab. `groupPostsByMonth` builds its keys through the same helper, so the two can't drift.
  - **Tooltips scale out of their anchor, with an over-extend** (components/ui/tooltip.tsx — app-wide, not deck-specific). The scale was never animating: the transition list said `transform`, but Tailwind's `scale-*` utilities set the standalone **`scale`** property, which is a different thing — so the bubble snapped to full size and only the fade ever ran. It now transitions `scale` (200ms) and `opacity` (130ms, plain strong ease-out) separately. `origin-(--transform-origin)` was already right and is kept: Base UI's Positioner publishes the anchor's own edge, which for a tooltip above its trigger resolves to `50% calc(100% + 16px)` — the sideOffset gap *below* the bubble — so it grows upward out of the tail and rises the last few pixels into place. Verified live on a settled tooltip: `transition-property: scale, opacity`, `transform-origin: 88.5px 52px` on a 36px-tall bubble.
    - The over-extend is a new `--ease-over-extend` in globals.css, a **`linear()`** curve peaking at 1.25. A cubic-bezier can't do this usefully: its overshoot is a fixed ~9% *of the animated range*, so on a 0.9 → 1 scale it lands under half a percent and is invisible. `linear()` sets progress point by point, so the peak is 25% past target regardless of range — here ≈ scale 1.025. **Opacity must never ride it** (it would pass 1, clamp, and flicker — the same reason toast.tsx keeps its fade off the spring), and neither does the exit, which is a plain 130ms ease-out to 0.96: an over-extend on the way out pulls the eye back to something that's leaving.
    - Debugging note: measuring a tooltip through `javascript_tool` shows `transition: none` inline, `data-starting-style` still attached and the element frozen at its starting scale. That's not a bug — Base UI applies the starting style with transitions disabled and clears both on the next frame, and **an eval backgrounds the tab, which suspends rAF**, so that frame never comes while you're looking. Hover with `computer` (which foregrounds), *then* measure the settled state.
  - **The cards' scroll container is the whole overlay**, not a box around the row — which is what stops the flights being clipped vertically. `overflow-x: auto` forces `overflow-y` to `auto` as well (an axis can't stay visible once the other scrolls), so that box clips vertically whether or not anything ever scrolls that way, and every card's flight starts and ends at the chip, which can sit hundreds of pixels above or below the row. Padding the box out (`py-pad-6xl -my-pad-6xl`, the first attempt) only ever buys a fixed amount of headroom — enough for the bounce's overshoot, nowhere near enough for a chip low on the page, where cards were visibly sliced on the way down. Making the container the viewport means the clip edge *is* the screen edge, and the chip is by definition on screen.
    - Two consequences to keep in mind. The container is `pointer-events-none` with the row `pointer-events-auto`, so a click on empty space still reaches the scrim and closes the deck, while a drag starting on a card still bubbles to the handlers on the container (verified: empty-space click closes; a 218px drag moved `scrollLeft` 312px from centre without closing). And the Close button is positioned, not stacked in a column with the row — `top: calc(50% + CARD_HEIGHT/2 + gap)`, both constants hand-synced with the card's own `h-98` and the old `gap-dist-4xl` (verified: cards centred in the viewport, button exactly 48px below them).
  - **Entrance animations are cancelled once they land** rather than left holding `fill: "both"` forever. The final keyframe is the element's resting state, so it changes nothing visually, but it stops a 23-card deck from keeping 23 filled animations (and their compositor layers) alive for as long as it's open. This is also the standing hypothesis for the reported *missing "Change date" label on some cards in a busy deck* — the label is a `TextMorph` (button.tsx) whose per-character spans each carry `will-change: opacity, transform`, so a big deck promotes a lot of layers at once and the text is exactly what a dropped raster tile would take out. **Unverified**: the browser extension disconnected mid-investigation, and a static read of torph's source found no path where the label's DOM nodes go missing on a label that never changes. If it recurs, the next lever is opting these buttons out of text morphing.
- Four follow-ups on Content, per direct request:
  - **A draft card in the deck now shows the actions menu too**, not the plain Trash button it used to. `PostActionsMenu`'s `onTurnToDraft` became optional alongside `onOpen`, so the same component covers both cases: a dated card gets Open up / Turn to draft / Delete, a draft gets Open up / Delete — there's no schedule to undo on a draft. `GeneratedPostCard` renders the menu whenever `date || onOpen`, which keeps the bare Trash button exactly where it belongs: the Generating page, whose cards have neither a date nor anywhere to open. Each row's `withDivider` is computed from what's actually above it rather than hardcoded, so a two-row menu doesn't open with a divider under nothing.
  - **The card header's date is a short date now** — `formatShortDate` in lib/format-date.ts, "Sept 15th, 2026". The month is spelled out in a local `SHORT_MONTHS` array rather than taken from `toLocaleDateString({ month: "short" })`, which gives "Sep"; the ordinal and year stay, since those are what make it a date rather than a label. Truncation backs it up (`min-w-0` on the header group, `shrink-0` on the icon, `truncate` on the date) so it can never take a second line no matter what's beside it.
  - **The tab is stored the same way the view is** — `getContentTab`/`setContentTab` in lib/content-view.ts, same per-project key shape, same listener set, read through the same `useSyncExternalStore`. It had been component state, so leaving for a post's page and coming back dropped you on Queued regardless of where you'd been; the page remounts on that navigation and state doesn't survive it.
  - **A Kanban card opens its page on the tap**, via a `loading.tsx` in `calendar/[postId]/`. Without one the router has nothing to commit to until the post's fetch returns, so the tap sat on the Content page doing nothing visible; a loading boundary lets it commit immediately and stand in meanwhile. It renders the `GlowPanel` shell around a `SectionSpinner`, not a bare spinner, so the post arrives into a frame that's already there. **Note this is a different mechanism from the sidebar's section overlay** (lib/section-navigation.ts) — that one exists precisely because `loading.tsx` alone commits too late for a *section* switch; here the segment below `calendar` is the thing being fetched, so the boundary is the right tool. Back gets the matching treatment in the other direction: `useTransition` + `router.push` with a `SpinnerGap` in the button while it's pending (a plain `<Link>` would show nothing at all for the gap), plus a `router.prefetch(backHref)` on mount, since arriving here through a menu item means nothing prefetched it. Verified by MutationObserver: the URL flips to the post id **with the loading spinner up and no heading**, then the real page replaces it; and the Back button carries `disabled` + `animate-spin` ~10ms after the click.
- Two more, per direct request:
  - **The Content page comes back where you left it.** `hooks/use-scroll-memory.ts` keeps each scroll container's offset in a **module-level Map**, not session/localStorage: a scroll position is only meaningful for the trip you're on (out to a post's page and back), so it should survive every client navigation and die with a real page load — which is exactly a module's lifetime. It also keeps the write cheap enough to do on every scroll frame, where a synchronous storage write would not be. Restoring happens in the **callback ref**, not an effect: React attaches refs bottom-up once the subtree's DOM exists, so the content that makes the element scrollable is already there and the assignment sticks.
    - **The key includes the layout and tab, and it must also reset to 0 when that key has nothing stored.** The two layouts are branches of a ternary that both render a `<div>`, so React *reuses the same DOM node* between them (verified: `sameNode: true` across a Kanban→Calendar switch) — without the reset, switching inherited the other list's offset and landed you somewhere nobody chose. A changed key re-runs the ref because React detaches the old callback and attaches the new one, which is also what makes one hook cover every tab's position.
    - Composed with `useScrollFade` at the call site (one `useCallback` calling both refs, one handler calling both `onScroll`s) — both want the same node and the same events.
  - An info line — `Info` icon + "Drafts sorted on date created", the same `body-md`/`text-subtle` shape the Generate and Generating pages use — sits under the segmented bar on the **Draft tab only**. It's the one tab whose chips aren't scheduling: drafts have no date, so they group by the day they were created (`groupingDate` in lib/content-grouping.ts), which is worth saying out loud. The `flex-col gap-dist-md` wrapper around the tabs row was already there from the removed "Tap on a day…" line.
  - **"Open up" closes the deck before the page changes.** It sets an `afterClose` ref and calls `startClose()`; the close effect's `finish` runs it after `onClose()`, so the cards fly home into their chip and *then* the post's page appears, rather than the whole overlay blinking out mid-flight. It also `router.prefetch`es on the way in, so the ~half second the put-away takes is spent fetching rather than added to the wait. A ref rather than state because nothing renders from it and the effect reads it at the end of an animation, long after the render that set it.
- Connections is built from the Figma `connect-base` + `social-connected` exports — social accounts only (the AI-models card moved wholesale to Settings: `components/connections/` → `components/settings/`, `connections/model-actions.ts` → `settings/model-actions.ts`, both `revalidatePath` targets repointed, since a BYOK key is user-scoped not project-scoped). **UI only, by request — nothing talks to LinkedIn and nothing persists**; "Connect" fills in a local placeholder account so the connected row can be seen, and a reload clears it. The exports replace UX doc §10.3's per-platform cards with 312px platform rows; see INTERFACE.md §9a for the full spec, and the "Hard constraint — publishing" section above for what must never be wired up. New: `components/shared/social-icon.tsx`, `components/connections/{platform-row,connected-account-row,connections-panel}.tsx`, `types/social-account.ts`, `formatExpiry` in lib/format-date.ts.
- Connections is now **wired to a real LinkedIn OAuth flow** (sign-in scopes only — `openid profile email`; `w_member_social` is deliberately not requested, and a test pins that, so the hard publishing constraint above still holds by construction). A connection is **project-scoped**: `public.social_accounts`, one row per (project, platform) (migration `create_social_accounts_table`, RLS mirroring writing_styles, access token encrypted with key-crypto.ts's AES-256-GCM envelope and never selected into anything the browser sees). Connecting is a browser redirect, so it lives in `app/api/connections/linkedin/{authorize,callback}/route.ts` rather than a server action — the authorize leg sets an httpOnly `sameSite: lax` state cookie carrying the project id, the callback compares it, exchanges the code, reads `/v2/userinfo`, upserts onto the `(project_id, platform)` unique constraint (so reconnecting replaces rather than stacks), and returns with `?connected=` / `?connect_error=<code>`; the page seeds a toast from those and strips them with `window.history.replaceState`. Disconnect is a server action (delete first, best-effort revoke after) with the app's optimistic-delete shape. **Needs `LINKEDIN_CLIENT_ID`/`LINKEDIN_CLIENT_SECRET` in `.env.local`** (see `.env.local.example`) — until they're set, Connect round-trips and reports "LinkedIn isn't set up yet". Verified end-to-end against the real API on 2026-08-21: connect, profile read, avatar render, and renew (which LinkedIn completes with no consent screen while the current token is still alive, upserting the same row in place). Only `disconnect`'s decrypt-and-revoke path is still unexercised live. The connected screen itself comes from the `connection-connected` export: same `EmptyState` as the empty one with `PlugsConnected`, a green "n connections active" badge in the caption slot (`EmptyState`'s `caption` is a ReactNode now), and its own title.
- Post cards now show the **real connected account** rather than the platform: `lib/post-account.ts` resolves a post to its account (brand icon + account name, e.g. "Godwin John"), falling back to the platform label when that platform isn't connected. No `social_account_id` on posts — `social_accounts` is unique on `(project_id, platform)`, so a platform already names exactly one account per project. The one thing that can't be derived is **"Try out"**, whose batches borrow LinkedIn as their platform, so migration `add_posts_is_tryout_column` adds `is_tryout boolean not null default false` and a try-out post renders as Eyes + "Try out" instead of the user's real name. The pill cycles only between connected accounts and degrades to a plain `<span>` when there's nowhere to go (one connected account is the common case). `Chip` gained a third palette, `retired` (`surface-4`/`border-minimal`/`text-minimal`), for a topic that has since been deleted from Instructions — `posts.topics` is a snapshot with no FK, so the chip stays but stops looking selectable; the Content page passes the project's current topics down as a Set. Also centralized the posts table's shape (`POST_COLUMNS`/`PostRow`/`mapPostRow` in queries.ts) — it was duplicated across six selects and four row-mappings. **All posts were deleted at the user's request** (430 rows) for a clean slate under the new model.

- Post-details rebuilt from the `contentpagenew` export: one **400px column centred in the panel with the heading, a new account/topics row, and the body all flush to that column's left edge** (it used to centre the heading independently, so it floated over the body). **"Try out" is now a position in the account pill's cycle**, not an exception — excluding it left the pill dead for anyone with a single connected account, which is the common case; a Try out position carries the post's own platform so switching to it and back is lossless, and `updatePost` writes `platform`/`isTryout` as a pair. The pill markup is now one shared component, `components/shared/post-account-pill.tsx` (button when there's somewhere to tap to, plain span when there isn't — which now only happens with no connected accounts). **DialKit is fully gone from the Content section** (post-details' three regeneration panels frozen to constants, same treatment as toast.tsx/use-shake.ts). The move-to-draft toast now fires on the click rather than on the server response — measured at 15ms vs the request's 1198ms.

- Regenerate can now change a post's topic (`regeneratemodalwithtopic`): a second SelectPill under the model one, offering the post's own topic then the project's. Regenerating is the right home for it — it's the one action that rewrites the post, so a new subject produces words that match; the route writes `topics` back only when the pick actually changed. Post-details topic chips also moved to `text-bold` (retired ones stay `text-minimal`).
- The in-project page scroll (`<main>`, every section) now fades at its top edge via `components/shared/section-scroll-area.tsx` — an overlay strip in the canvas colour, **not** a `useScrollFade` mask, because a mask paints out the `position: fixed` toasts five sections render inside `<main>` (see LEARNINGS). No bottom fade: that edge deliberately bleeds to the screen.
- Dashboard built from the `dashboarddesign`/`-2` Figma exports (INTERFACE.md §12a): Total-posts card (cumulative Draft/Queued/Published bar + percentage marker) over three mini stat cards, three stat cards, a month grid, a scrolling Next-up/Recently-out column, topic chips + per-platform bars, and a Setup checklist with badge/pill tones. `lib/dashboard-summary.ts` derives every figure from `posts` alone, date-derived like `lib/content-grouping.ts` (never `posts.status`, which never says "published"). The zero-posts `EmptyState` is untouched. **No engagement metrics** — the app doesn't publish and reads nothing back. The "Try out" row is a real count: `platformSplit()` partitions on `isTryout` *before* platform (the same precedence `lib/post-account.ts` uses), so LinkedIn/X/Try out are disjoint and sum to the total rather than filing try-out posts under LinkedIn.
- Two small fixes, no branch: (1) **"Try out" is now a Content-page filter value** — `PlatformFilter` gained a `"tryout"` position, the Social row cycles all → LinkedIn → X → Try out → all, and matching goes through `matchesPlatformFilter` (lib/content-filter.ts), which checks `isTryout` *before* platform, so picking LinkedIn no longer returns the try-out posts that borrow it. The menu row draws the Eyes glyph for it, as every other try-out affordance does. (2) **The dashboard's "What you're posting about" was showing Try out at 0** — `platformSplit` was fine; it was being handed `monthPosts`, which is scheduled-only, and a try-out post is normally dateless (the common Try out path is number-based, which schedules nothing — though calendar-based generation with the pill on Try out does produce dated ones). New `postsInMonth` (lib/dashboard-summary.ts) takes a scheduled post by its date and a dateless one by when it was written — the Draft tab's own rule — and the card (chips and bars both) reads that instead. The calendar and coverage figures keep the scheduled-only set.
- Dashboard, three items: (1) **empty Next-up/Recently-out sections** built from `design-sync/emptydashboardpostsection` — a fixed 200px `surface-2` `rad-xmd` tray (squircled) holding the empty-state block, replacing the grey one-liner; (2) **`EmptyState` gained `size="sm"`** (32px icon, `dist-md`, `title-lg`, title as `<p>` not `<h1>` since it sits under a heading) so one component serves both the page-sized blank state and this in-tray one; (3) **section spacing is `dist-2xl`** — DashboardView's top-level gap and the gap between the two post sections — and the zero-posts dashboard's **"Get started" button is removed** (never wired to anything). See INTERFACE.md §12b.
- **The app chrome locks while a generation runs.** Leaving the generating page unmounts the view, and that unmount is what ends the run — so `lib/generation-lock.ts` (a module store, same shape as network-status/section-navigation) is set by GeneratingView for as long as `status === "generating"`, and `hooks/use-generation-lock.ts` has ProjectSidebar and ProjectsNavbar render at `opacity-40` and `inert` (not `pointer-events-none` — a keyboard user could still tab into it and navigate away). Released on Stop, on completion, and on unmount. Verified in-browser; `lib/generation-lock.test.ts` pins the store.
<!-- END:presto-agent-rules -->
- **The account screen has a user-level route.** `/profile` renders the same screen as `/projects/<id>/profile` — nothing on it is project-scoped — but with the picker's chrome (ProjectsNavbar, `backHref="/projects"`) and no sidebar, instead of forwarding into the user's first project the way it used to. `components/profile/profile-content.tsx` is the shared server component both routes render; `projectId` is now optional all the way down (ProfileScreen → EditableName/ChangePasswordPanel/AiModelsPanel → `updateDisplayName`/`addUserAiModel`/`deleteUserAiModel`), where it only picks which path a write revalidates. "Replay onboarding" is in-project only — the tour narrates the sidebar, and there isn't one out here. Top-level `/settings` now forwards to `/profile`. Deliberately a page, not a modal: the screen is expected to grow and a dialog holding more than this would be unusable on a phone.
- **BYOK no longer goes through the Vercel AI Gateway** (supersedes the BYOK bullet above): user keys now call providers directly via `lib/ai/providers.ts`, a `DirectProvider` registry (`listModels` + `languageModel` per provider). **Four providers are implemented — Anthropic, Google, Groq, OpenAI**; adding one is a registry entry plus its `@ai-sdk/*` package. Each needs a **maintained model blocklist**, because a provider's capability metadata is not a modality filter: 39 Google models report `generateContent` and only 20 can write a post, and neither `nano-banana-pro-preview` (image) nor `lyria-3-*` (music) names its modality in its id. All three filters are pinned by `lib/ai/providers.test.ts`. The gateway was dropped because its BYOK mode requires *us* to hold paid credits, and because it silently served free-tier-eligible models on its own account — making a fake key look valid (LEARNINGS.md). Consequences: no `AI_GATEWAY_API_KEY`, and no fallback detection (`didFallBackOffByok`/`confirmedRanOnByok` deleted — a direct call can't fall back). Key verification is free: listing models with the key *is* the check, so nothing is billed and a valid-but-unfunded key can still be saved. Stored ids stay prefixed (`anthropic/claude-sonnet-5`) and the `gateway_model_id` column keeps its now-misnomer name — renaming buys nothing and schema changes are serialized. **Packages added (7):** `@ai-sdk/anthropic`, `@ai-sdk/openai`, `@ai-sdk/groq`, `@anthropic-ai/sdk` and `openai` (the last two only for `models.list()`; Groq reuses the `openai` client at its own `baseURL` rather than adding a third), plus `unpdf` and `mammoth` for document text.
- **Client components must import AI constants from `lib/ai/model-constants.ts`, never `lib/ai/generate.ts`.** `generate.ts` imports `providers.ts`, which builds its provider objects at module scope, so nothing tree-shakes — five client components importing one string took `.next/static/chunks` from 3.7 MB to 6.9 MB with a 1.07 MB chunk on four routes. tsc, eslint, vitest and `next build` were all green throughout; only diffing built bundles caught it. `lib/ai/no-client-sdk.test.ts` now fails if a client component reaches a server-only AI module.
- **`google.tools.urlContext` is deleted, and that is a real trade-off, not a clean win.** URL entries in Instructions used to be fetched by Google on the model's behalf, which only ever worked on the built-in; every other provider was handed a URL it could not read. They are now fetched server-side (`lib/ai/fetch-url.ts`) and inlined as text, so all four providers behave identically — but **URL handling got weaker for the built-in Gemini**, which lost Google's fetching. A plain `fetch` + tag-strip cannot read a JS-rendered page, and gets served a block page where Google's crawler would not. If a URL reference ever silently produces nothing useful, this is why.
- **X publishing is built and switched off; X connecting is "Coming soon".**
  `lib/x/publish.ts` mirrors the LinkedIn one (same gate, same
  `published_without_urn` hazard) and was exercised against the live API, but X
  meters posting **per app across every user of the app**, not per account, and
  the first real send returned `402 credits depleted`. The owner's decision
  (2026-09-04) was not to pay, so four switches are off: `PUBLISHABLE_PLATFORMS`
  (lib/post-publish.ts), `X_SCOPES` (no `tweet.write`), `available` in
  connections-panel.tsx, and the X app's own permission. Tests pin the first
  two. Nothing is deleted — turning it on is those switches plus a reconnect.
  `lib/publish-runner.ts` dispatches per platform, and its refusal, the length
  guard and every "coming soon" affordance are described in INTERFACE.md §10b.
- `disconnect`'s decrypt-and-revoke path **is now exercised live** (2026-09-01,
  `feat/connection-expiry`), superseding the "still unexercised" note earlier in
  this list: revoking really does destroy the grant at LinkedIn's end. Also
  learned the hard way — **the OAuth connect leg only works on :3000**, the one
  redirect URI registered on the LinkedIn app; from a worktree port it dies at
  LinkedIn with "redirect_uri does not match the registered value". Server-to-
  server calls are unaffected on any port. See LEARNINGS.md.
- **Deployed to Vercel, 2026-09-06 — `https://presto.godwinjohn.com`** (project
  `presto`, team `gdwn`, Hobby plan). The origin is a subdomain rather than the
  auto-generated `*.vercel.app` on purpose: the project's `ssoProtection` is
  `all_except_custom_domains`, so the custom domain is public while every
  preview deployment stays private to the account. **Publishing is ON as of
  2026-09-07** — `PRESTO_ENABLE_LIVE_PUBLISH=true` in **production only**
  (never Preview: preview runs against this same production database), verified
  on the deployed build with the scheduler's own record catching the flip —
  ticks at 06:48/06:49Z reported `false`, 06:50Z onward `true`, every one
  `published: 0`. Read the "Publishing is LIVE" section above before touching
  anything that sends. The `presto-publish-due` pg_cron job runs
  every minute against the deployed origin and reads its secret from Supabase
  Vault. **Deploys are automatic as of 2026-09-07**: the project is connected to
  `JGodwyn/Presto` with `main` as the production branch, so **anything that
  lands on `main` ships to the live origin** — including the small housekeeping
  commits the "Parallel work" section above allows straight on `main`. Verified
  end to end (`source: git`, `READY / PROMOTED`, holding
  `presto.godwinjohn.com`). `vercel deploy --prod` still works for an
  out-of-band deploy. The LinkedIn
  production callback **is** registered (proved 2026-09-07 by probing LinkedIn's
  authorize endpoint against a control — see FOLLOWUPS §5.1), and production
  carries a live, active LinkedIn connection. Note `vercel link` appends a
  `VERCEL_OIDC_TOKEN` line to `.env.local` (gitignored, harmless).
